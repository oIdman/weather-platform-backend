# 工作流完整移植进度

本文持续记录从 `ruoyi-vue-pro/yudao-module-bpm` 与 `yudao-ui-admin-vben/apps/web-antd/src/views/bpm` 向 JeecgBoot 的移植进度。每次产生实质性代码更新时，必须补充“本次更新、验证结果、已知差距、后续目标”。

## 最终目标

- 后端能力以 `E:\user\Desktop\Project\ruoyi-vue-pro\yudao-module-bpm` 为基准，使用独立的 `jeecg-module-workflow` 模块承载，并通过适配器接入 JeecgBoot 用户、租户、权限、文件与消息能力。
- 前端功能与菜单以 `E:\user\Desktop\Project\yudao-ui-admin-vben\apps\web-antd\src\views\bpm` 为基准；界面样式允许遵循 JeecgBoot 现有组件体系，不要求像素级一致。
- 全局设计与迁移边界参考 `E:\user\Desktop\Project\yudao-ui-admin-vben\docs\jeecgboot-workflow-integration-guide.md`。
- 移除原气象科研业务模块中硬编码的 Flowable 流程定义与自动部署，业务记录只通过 `businessKey`、流程变量和适配接口关联工作流。

## 当前菜单基线

当前数据库迁移已建立与目标前端一致的两组菜单：

- 流程管理
  - 流程模型
  - 流程表单
  - 流程分类
  - 用户分组
  - 流程监听器
  - 流程表达式
  - 流程实例
  - 流程任务
- 审批中心
  - 发起流程
  - 我的流程
  - 待办任务
  - 已办任务
  - 抄送我的

## 2026-09-05：基础闭环、简单设计器与旧流程清理

### 本次更新

- 建立可移植的后端模块边界：`workflow-api` 保存 DTO/VO 和适配契约，`workflow-biz` 保存 Flowable 编排、Controller、Service、Mapper 与 JeecgBoot 适配实现。
- 增加流程分类、表单、用户分组、监听器、表达式、模型、定义、实例、任务、抄送和评论的基础 REST API，并保持 `/bpm/**` 路由族。
- 增加 JeecgBoot 用户与租户适配，流程部署、查询和运行数据按租户隔离。
- 增加简单流程设计器前端，可新增审批、条件、并行、包容、抄送、触发器和子流程节点，并保存 Yudao 简单模型 JSON。
- 增加简单模型 JSON 到 Flowable BPMN 的转换器，已支持基础审批链、条件/并行/包容网关、发起人本人候选人与子流程调用。
- 修复模型发布请求参数、发起流程列表重新查询，以及流程详情页因路由生命周期导致的空白问题。
- 修正旧 MRP 流程清理逻辑：仅删除名称为 `SpringBootAutoDeployment` 且全部流程 key 都属于旧 MRP 清单的部署，避免误删用户流程。
- 删除旧 MRP classpath BPMN 文件与自动部署 Runner；原业务表保留流程实例关联字段，后续统一通过工作流模块调用。

### 验证结果

- 后端工作流单元测试：5 个测试通过，覆盖 BPMN 校验器与简单模型转换器。
- 后端完整 Maven 打包：`BUILD SUCCESS`，`jeecg-system-start-3.9.5.jar` 已重新生成。
- 前端 ESLint：本轮修改的工作流 API、设计器、模型、发起页和详情页均通过。
- 前端生产构建：成功；仅保留仓库原有的主题插件可选依赖和样式压缩警告。
- 浏览器端已跑通：创建简单模型 → 图形化编辑 → 保存 → 发布 → 发起流程 → 待办详情 → 审批通过 → 实例完成。
- 旧 MRP 自动部署已由启动清理器识别并删除；用户创建的 `integration_verify` 验证流程未被清理。

### 已知差距

- 当前后端是按 RuoYi/Yudao 接口语义重新适配的 JeecgBoot 实现，不是 `yudao-module-bpm` 的逐文件复制；高级行为仍未达到完整等价。
- 简单设计器的抄送节点、触发器节点目前尚未接入真实运行时行为。
- 会签/或签、多实例完成条件、完整候选人策略、复杂条件组、节点监听器和任务超时等配置尚未完整转换到 BPMN。
- BPMN 图形信息（DI）和目标前端的完整模型配置面板尚未齐全。
- 流程详情、表单渲染、签名、附件、打印、流程报表及高级任务操作仍需逐项与目标工程核对。
- 独立服务启动器以及远程用户、租户、文件、消息适配尚未完成，目前模块可拆分但仍随 JeecgBoot 单体启动。

### 后续目标

1. 建立后端 Controller/Service/DTO/行为矩阵，对 `yudao-module-bpm` 的公开接口逐项补齐并增加兼容测试。
2. 补齐简单设计器运行语义：候选人策略、条件组、多实例、抄送、触发器、监听器、超时与子流程。
3. 对照目标 `views/bpm` 完成模型配置、流程详情、表单、任务操作、打印和报表页面的功能矩阵。
4. 提供独立 Spring Boot 启动器与 adapter SPI，实现单体嵌入和独立服务两种部署方式。
5. 完成全菜单、全页面、全接口的端到端回归后，再清理验证数据并形成部署、升级和回滚说明。

## 2026-09-05：流程定义接口与发起权限对齐

### 本次更新

- 对照目标 `BpmProcessDefinitionController` 补齐 `GET /bpm/process-definition/list`、`simple-list` 和 `get`，当前目标流程定义 Controller 的公开查询路由已覆盖。
- 流程定义响应补充 `categoryName`、`suspensionState`、`modelType`、`modelId`、`sort`、`bpmnXml` 和 `simpleModel` 等目标页面依赖字段。
- `list` 只返回当前租户中当前用户可发起的流程：先校验流程可见性，再按目标语义依次校验允许用户和允许部门。
- 流程实际发起时复用同一权限校验，避免只依赖前端列表隐藏。
- JeecgBoot 身份适配器新增当前用户部门查询，调用宿主 `ISysBaseAPI.getDepartIdsByUsername`，工作流核心不直接访问 JeecgBoot 用户表。
- 前端新增独立的 `src/api/bpm/definition` API 模块，发起流程页切换为目标前端同款的激活流程列表接口。

### 验证结果

- 工作流后端及依赖模块编译成功。
- JeecgBoot 启动模块重新打包成功，后端重新监听 8080。
- 新增前端 API 和发起流程页通过 Prettier 与 ESLint。
- 实际 HTTP 验证：`list`、`simple-list`、`get` 均成功；租户 1000 返回 1 个激活定义，详情包含 BPMN XML、模型类型和挂起状态。

### 已知差距

- 本轮首次 HTTP 验证发现定义详情的 `simpleModel` 未返回，原因是按部署查模型时未加载编辑器附加源；改为加载完整模型源后已重新打包并二次验证，当前接口能够返回该字段。
- 流程定义分页的筛选字段仍少于目标 `BpmProcessDefinitionPageReqVO`，管理页的名称、分类、标识、状态等组合筛选尚需补齐。
- 发起人自选审批人及“下一审批节点”接口尚未实现。

### 后续目标

1. 增加流程定义接口级自动化测试。
2. 补齐发起阶段的审批路径预测与发起人自选审批人界面。
3. 继续对齐流程实例和任务 Controller 的请求参数、响应字段及状态语义。
4. 按目标前端 108 个 Vue 文件建立页面/组件矩阵，优先补齐模型配置与流程详情模块。

## 2026-09-06：下一审批节点预测与自选审批人运行变量

### 本次更新

- 补齐目标前端依赖的 `GET /bpm/process-instance/get-next-approval-nodes`，接口先校验任务属于当前用户，再按租户校验运行中的流程实例。
- 新增 Flowable BPMN 导航器，可从当前任务递归跳过网关、服务任务和中间事件，定位下一批用户任务；排他、包容和并行网关分别按流程变量、默认分支和并行出口预测。
- 下一节点响应对齐目标字段：`id`、`name`、`nodeType`、`status`、`tasks`、`candidateStrategy`、`candidateUsers` 和 `processInstanceId`。
- 候选审批人已适配 JeecgBoot 用户体系，当前支持指定角色、部门成员、部门负责人、岗位、指定用户、发起人自选、审批人自选、发起人本人和发起人部门负责人等策略的基础解析。
- 简单模型转 BPMN 时写入 `candidateStrategy`、`candidateParam` Flowable 扩展元素，不再只把候选规则隐含在 assignee/candidateUsers 字段中。
- 流程发起请求兼容目标字段 `processDefinitionId` 和 `startUserSelectAssignees`，同时保留原有按流程 Key 发起方式；业务标识和实例名称改为可选，未传名称时使用流程定义名称。
- 审批请求新增 `signPicUrl`、`attachments` 和 `nextAssignees`；审批人自选结果会合并写入流程变量 `PROCESS_APPROVE_USER_SELECT_ASSIGNEES`，供后续节点 assignee 表达式读取。
- 当前审批详情页在点击“通过”时调用下一节点预测接口；遇到候选策略 34（审批人自选）且尚无候选人时，动态显示 JeecgBoot 用户选择控件并随审批请求提交。

### 验证结果

- 工作流模块及依赖执行 `compile`，结果 `BUILD SUCCESS`。
- 定向执行 `WorkflowSimpleModelConverterTest`，3 个测试全部通过；新增覆盖候选策略扩展元素、候选参数和发起人自选 assignee 表达式。
- 全 Reactor 测试曾实际启动，但在上游 `jeecg-boot-base-core` 的 3 个既有 SSRF 公网域名解析用例失败后停止；失败发生在工作流模块测试之前，与本次工作流代码无关。随后采用定向测试完成本批验证。
- 本次修改的 `workflow.api.ts` 与流程详情页已通过 Prettier 和 ESLint。

### 已知差距

- 发起人自选（策略 35）的后端变量和 BPMN 执行表达式已具备，但发起流程页尚未像目标前端一样先预测审批路径并展示逐节点选人控件。
- 当前自选审批人的执行表达式先使用所选列表中的第一个用户；目标项目的会签/或签会根据节点多实例配置处理多个用户，本实现尚未对齐该部分。
- 角色、部门、岗位等候选策略的 ID/编码映射仍需与 JeecgBoot 实际主数据约定做端到端验证；用户组、表单字段、表达式和连续多级部门负责人策略尚未完整实现。
- 下一节点预测已覆盖常见网关，但尚未覆盖子流程内节点、多实例节点和边界事件的目标完整语义。
- 签名和附件字段已进入请求契约，尚未保存为任务局部变量并回显到审批详情。

### 后续目标

1. 扩展发起阶段 `get-approval-detail` 兼容接口，按流程定义和表单变量预测审批路径，并在发起页完成策略 35 的逐节点选人。
2. 补齐会签、或签、多实例完成条件，让多个自选审批人都产生符合目标语义的任务。
3. 把签名、附件、审批状态和意见持久化到任务局部变量，并对齐详情时间线响应。
4. 继续迁移目标流程详情的时间线、流程图、表单权限和操作按钮子模块。

## 2026-09-06：发起前审批预测与发起人自选界面

### 本次更新

- 将 `GET /bpm/process-instance/get-approval-detail` 的请求契约对齐目标 `BpmApprovalDetailReqVO`：支持 `processDefinitionId`、`processVariables`、`processVariablesStr`、`processInstanceId`、`activityId` 和 `taskId`，并校验流程定义编号与流程实例编号至少存在一个。
- 保留已发起流程的审批详情查询，同时新增“未发起”分支：按当前租户和当前用户校验流程定义及发起权限，从 BPMN 开始节点预测首批审批节点。
- 审批详情响应新增目标前端依赖的 `status`、`activityNodes` 和 `formFieldsPermission` 字段；未发起流程按目标枚举返回状态 `-1`，不再伪造流程实例。
- 统一审批变量解析逻辑；`processVariablesStr` 会解析并覆盖预测变量，条件网关可以在发起前根据申请表单值选择分支。
- 发起流程页在选择流程定义后立即加载审批预测，筛选候选策略 `35`（发起人自选），为每个节点显示 JeecgBoot 用户选择器。
- 发起弹窗新增“预计审批路径”纵向步骤视图，展示发起人、全部可达审批节点、候选审批人提示和结束节点；样式采用 JeecgBoot/Ant Design，但信息结构与目标发起页时间线一致。
- 正式发起前会使用最终表单变量重新预测路径，校验每个发起人自选节点均已选择审批人，并通过 `startUserSelectAssignees` 提交到后端。
- 区分“下一节点”和“完整可达路径”两种 BPMN 导航：任务通过时只返回下一批用户任务，发起前则继续穿过后续用户任务，保证非首节点的策略 `35` 也会要求发起人选择审批人。
- 流程详情页切换为目标接口参数 `processInstanceId`，同时修正新增用户选择器的 `multiple` 属性类型，消除该处布尔值透传告警。
- 转换器回归测试增加从 BPMN 开始事件预测到“发起人自选”用户任务的断言，确保模型转换和审批预览使用同一节点编号。

### 验证结果

- 工作流后端及依赖模块执行 `compile`，结果 `BUILD SUCCESS`。
- 在增加“完整可达路径”之前，定向执行 `WorkflowSimpleModelConverterTest`，3 个测试全部通过，已覆盖从开始事件到首个审批节点的预测；随后新增的“继续穿过首个用户任务并发现后续自选节点”断言因构建额度限制尚待复跑。
- `workflow.api.ts`、发起流程页和流程详情页执行 Prettier 与 ESLint，检查通过；之后仅追加了流程详情的实例空值保护和选择器属性类型修正，最终静态检查待额度恢复后复跑。
- `git diff --check` 未发现空白错误；输出仅包含仓库既有 CRLF 转换提示。
- 尝试重新打包单体启动模块以进行运行态验证时，被当前 Codex 主机的用量限制拒绝；现有 8080 进程仍是上一版 JAR，因此本批接口尚未声明完成运行态验证。

### 已知差距

- 当前发起前审批预览已返回条件路径上的全部可达用户任务；目标实现还会组合开始/结束、抄送、触发器、已完成、运行中、退回补偿和子流程节点，完整时间线仍需继续迁移。
- `formFieldsPermission` 已建立响应字段但目前为空，尚未从 BPMN 节点扩展属性解析发起人/审批人字段权限。
- 发起页已具备策略 `35` 的实际选人闭环，但尚未用新打包 JAR 完成浏览器端端到端验证。
- 会签/或签仍只消费自选列表的第一个用户，多个审批人的 Flowable 多实例语义尚未实现。

### 后续目标

1. 在构建额度恢复后重新打包并重启后端，浏览器验证“模型发布 → 发起前预测 → 自选审批人 → 生成待办”的完整路径。
2. 对齐目标 `getApprovalDetail` 的完整活动节点时间线，合并已完成、运行中和模拟未运行节点以及任务人员信息。
3. 从 BPMN 扩展属性解析并执行表单字段权限、按钮配置、退回策略、超时策略和节点监听器。
4. 迁移目标流程详情的时间线、签名、附件、流程图、打印和操作按钮组件，并逐项建立接口/页面回归矩阵。

## 2026-09-06：审批签名与附件持久化闭环

### 本次更新

- 审批通过和拒绝时，把目标项目同名的任务局部变量 `TASK_STATUS`、`TASK_REASON`、`TASK_SIGN_PIC_URL` 写入 Flowable 历史数据；加签子任务也经过同一持久化路径。
- 使用 Flowable 原生 Attachment 保存审批附件，附件类型保持目标项目的 `1`；任务列表与详情返回新增 `resultStatus`、`reason`、`signPicUrl` 和 `attachments`。
- 历史任务和当前待办查询启用 task-local variables，避免任务完成后签名、意见和状态无法回显。
- 对签名和附件地址增加服务端白名单校验，仅允许站内路径及 HTTP/HTTPS，并限制长度和换行字符，避免把不可信协议带到详情页。
- 流程详情审批/拒绝弹窗接入 JeecgBoot 的图片上传和文件上传组件，上传结果随任务操作提交。
- 审批时间线新增处理意见、签名预览和附件链接；前端再次校验 URL 协议，并为新窗口链接设置 `noopener noreferrer`。

### 验证结果

- 本批修改已完成源码级接口核对和 `git diff --check`；由于当前 Codex 主机构建额度仍受限，尚未执行本批 Maven 编译、前端 ESLint 和浏览器端上传验证。
- 上一批工作流模块编译、定向测试及前端静态检查结果仍有效，但不覆盖本批新增代码。

### 已知差距

- 当前附件查询按任务逐条读取，功能已闭环但列表量大时存在 N+1 查询；目标实现会按流程实例批量查询并分组。
- 当前签名使用 JeecgBoot 图片上传，尚未迁移目标前端的手写签名画布组件。
- 审批状态已保存为目标枚举值，但拒绝后直接结束/退回到指定节点等目标策略仍未完整对齐。

### 后续目标

1. 构建额度恢复后编译、静态检查并实际验证签名及附件上传、审批、历史回显。
2. 把任务附件查询改为按流程实例批量加载，组装完整 `ActivityNodeTask` 时间线。
3. 迁移目标手写签名组件、任务证据组件和打印视图。
4. 继续补齐拒绝、退回、撤回和加签的状态机与目标行为。

## 2026-09-06：审批活动节点时间线组装

### 本次更新

- 已发起流程的 `get-approval-detail` 不再只返回平铺任务列表：读取完整历史变量，以请求变量为优先，并按实际 BPMN 条件路径计算活动节点。
- `activityNodes` 现在依次组装发起人节点、路径内审批节点、历史补偿节点和成功结束节点；节点新增 `startTime`、`endTime`，并按任务局部状态返回 `-1/1/2/3/4`。
- 相同 BPMN 用户任务产生的会签、循环或加签任务按 `taskDefinitionKey` 聚合到同一个活动节点，签名、附件、意见等任务证据随节点返回。
- 对退回后不再属于当前预测路径但真实存在的历史任务进行补充，避免详情时间线丢失实际审批记录。
- 发起前审批详情也补齐发起人和结束节点，前端不需要自行猜测响应是否包含时间线边界节点。
- 拒绝流程不重复追加“结束”节点，与目标前端用拒绝节点直接显示终态的行为保持一致。
- 流程详情的审批进度和横向流程摘要已切换为消费 `activityNodes`，按节点显示状态、候选人、聚合任务、处理意见、签名和附件，不再用平铺任务列表猜测流程路径。

### 验证结果

- 完成源码级路径、状态枚举和目标实现核对；`git diff --check` 未发现空白错误。
- 当前构建额度限制尚未解除，本批活动节点组装和上一批签名附件代码均等待 Maven 编译、前端检查及运行态验证。

### 已知差距

- 当前路径模拟只返回用户任务；抄送、触发器、延时器和子流程活动尚未进入完整时间线。
- 历史补偿节点当前追加在预测路径之后；复杂退回和循环场景还需结合 HistoricActivityInstance 按真实发生时间排序。
- 活动节点任务暂时复用 `WorkflowTaskVO`，尚未拆成与目标 `ActivityNodeTask` 完全相同的专用响应类型和用户对象。

### 后续目标

1. 增加 HistoricActivityInstance 组装，覆盖开始/结束、子流程和真实活动顺序。
2. 拆分 `ActivityNodeTask` VO，补齐 ownerUser、assigneeUser、任务状态、签名和附件的目标字段。
3. 继续迁移目标时间线的节点状态图标、用户头像、子流程入口和并行分支视觉结构。
4. 恢复构建后集中修正编译/类型问题并完成端到端回归。

## 2026-09-06：菜单现场核验与 Jeecg 文件地址兼容

### 本次更新

- 直接核验当前 `jeecg-boot` 数据库：工作流根菜单下已存在“流程管理”和“审批中心”，分别挂载 8 个和 5 个可见页面菜单；相关父级、页面和按钮权限均已授权给当前管理员角色。
- Flyway `20260904.1` 至 `20260904.7` 均执行成功，因此“只显示工作流程一级菜单”的截图属于旧登录菜单缓存或迁移前状态，不是当前菜单表结构。
- 核对 JeecgBoot 原生 `JUpload`、`JImageUpload` 的值契约，确认上传组件保存 `temp/...` 等相对文件路径，而不是强制保存 `/...` 或完整 HTTP 地址。
- 审批签名和附件服务端校验现已兼容安全的 Jeecg 相对文件路径，同时继续拒绝协议相对地址、反斜杠、带协议冒号、`..` 路径段、超长地址和换行字符。
- 流程详情对安全相对路径统一调用 JeecgBoot `getFileAccessHttpUrl` 转换为文件访问地址；HTTP/HTTPS 和安全的站内绝对路径保持原样。

### 验证结果

- 已通过 MySQL 只读查询确认菜单层级、管理员角色授权和 Flyway 执行状态。
- 已通过源码核对确认两个 Jeecg 上传组件都把服务端 `response.message` 作为字段值回传。
- 本批地址兼容修改已执行 `git diff --check`；Maven 编译、前端 ESLint 和浏览器端上传回显仍需随上一批集中验证。

### 已知差距

- 现有工作流后端仍是面向 JeecgBoot 的 Flowable 适配模块，并非 `ruoyi-vue-pro/yudao-module-bpm` 的完整同构实现；目标后端约 243 个 Java 文件，当前模块约 81 个 Java 文件，完整能力仍在迁移。
- 现有 BPM 前端只迁入了核心页面，尚未达到 `yudao-ui-admin-vben/apps/web-antd/src/views/bpm` 的完整页面与组件数量；菜单名称已对齐主要信息架构，但页面功能仍需逐项对齐。
- 用户重新登录前，JeecgBoot 前端仍可能继续使用登录时缓存的旧权限菜单。

### 后续目标

1. 恢复构建后集中执行工作流模块编译、定向测试、前端 Prettier/ESLint，并重启后端验证最新接口。
2. 浏览器端验证重新登录后的 8+5 菜单结构，以及签名/附件上传、审批和历史回显。
3. 继续以目标 Controller/VO/页面矩阵为基准补齐流程实例、任务状态机、多实例会签/或签和完整详情组件。
4. 最终把工作流模块的 Jeecg 身份、租户、文件和菜单适配边界收敛为可移植服务接口。

## 2026-09-06：最新工作流代码集中构建与运行态验证

### 本次更新

- 对发起前审批预测、发起人自选、审批签名与附件、活动节点时间线及 Jeecg 文件路径兼容代码完成集中构建验证。
- 停止先前占用启动 JAR 的旧后端进程，重新打包 `jeecg-system-start`，并使用 `dev` 配置启动包含最新工作流代码的新 JAR。
- 新后端启动时成功连接 MySQL 和 Redis，校验 28 个 Flyway 迁移，确认数据库版本为 `20260904.7`，并成功创建 Flowable `ProcessEngine`。
- 启动过程自动清除了全部用户的 Shiro 授权缓存，减少角色菜单更新后仍读到旧权限的情况。
- 在当前管理员登录会话中完成浏览器实测：工作流根菜单可展开“流程管理”和“审批中心”；前者实际显示 8 个页面菜单，后者实际显示 5 个页面菜单。
- 浏览器成功打开 `/bpm/task/create` 发起流程页；页面正常调用新后端并显示“暂无可发起的流程”。当前数据库没有已发布且可由当前用户发起的定义，因此尚未进入本次新增的审批路径预测与自选审批人交互。

### 验证结果

- `mvn -f jeecg-boot/pom.xml -Pdev -pl jeecg-boot-module/jeecg-module-workflow/workflow-biz -am -DskipTests compile`：`BUILD SUCCESS`，工作流 API 38 个源文件、业务模块 41 个源文件编译通过。
- 显式覆盖父 POM 的默认跳过测试配置后执行 `WorkflowSimpleModelConverterTest`：3 个测试、0 失败、0 错误、0 跳过。
- `workflow.api.ts`、发起流程页、流程详情页执行项目 Prettier 和 ESLint：通过。
- `mvn -f jeecg-boot/pom.xml -Pdev -pl jeecg-module-system/jeecg-system-start -am -DskipTests=true package`：释放旧进程的 JAR 文件锁后 `BUILD SUCCESS`，Spring Boot 可执行包重打包成功。
- 新后端运行会话保持存活，Tomcat 正在 `8080` 端口监听，应用上下文为 `/jeecg-boot`。
- 浏览器辅助功能树直接确认 8+5 菜单名称与路由页面均已渲染；发起流程页面无前端崩溃或菜单组件解析错误。

### 已知差距

- 数据库当前没有用于端到端验证的已发布流程，尚未在运行态证明“选择流程 → 预测完整审批路径 → 发起人自选 → 创建待办 → 上传签名/附件 → 审批 → 历史时间线回显”的整条链路。
- 本次只对工作流模块执行定向测试；全仓测试未重跑，仓库既有 SSRF 网络解析测试仍可能受当前网络环境影响。
- 当前核心页面可以运行，但与目标前端 108 个 Vue 文件相比仍缺少大量详情子组件、多实例配置、打印、流程图和高级节点配置。

### 后续目标

1. 创建专用测试流程模型并发布，完成发起、预测、自选审批人、待办审批、签名附件和历史回显的端到端回归。
2. 补齐会签、或签、多实例完成条件和候选人策略，使多个自选审批人按目标语义生成任务。
3. 引入 `HistoricActivityInstance` 与专用 `ActivityNodeTask` 响应，完善真实活动顺序、退回循环、子流程和并行分支时间线。
4. 继续按目标后端 Controller/VO/Service 及目标前端页面组件矩阵补齐剩余能力，直到菜单与界面功能逐项验收一致。

## 2026-09-07：流程状态机、列表筛选与最新运行包验证

### 本次更新

- 新增与 `ruoyi-vue-pro` BPM 模块一致的流程实例状态变量：运行中 `1`、审批通过 `2`、审批不通过 `3`、已取消 `4`，并补充流程原因变量；发起、拒绝、取消和自然完成现在经过同一状态生命周期。
- 拒绝处理改为记录任务结果、意见、签名和附件，标记父任务，并把当前全部活动节点移动到流程唯一结束事件；取消流程记录取消原因后再终止实例。
- 新增 Flowable 流程完成事件监听器；首次整包启动发现其直接注入 `RuntimeService` 会参与引擎创建并形成循环依赖，已改为 `ObjectProvider` 延迟获取，避免为绕过问题而开启全局循环依赖。
- 流程实例列表新增名称、定义标识、流程状态、分类、创建/结束时间、发起人和表单变量筛选；任务列表新增名称、分类、定义标识、任务状态、流程状态和创建时间筛选。
- 前端为“我的流程”“流程实例”“我的待办”“我的已办”“任务管理”补齐对应查询表单，并以重复参数方式序列化时间范围，匹配 Spring MVC 数组绑定。
- 流程与任务列表补齐审批结果、审批意见和流程实例状态显示，字段契约与目标前端的核心列表行为进一步对齐。

### 验证结果

- 工作流业务模块定向执行 `WorkflowProcessInstanceEventListenerTest` 和 `WorkflowSimpleModelConverterTest`：5 个测试、0 失败、0 错误、0 跳过，`BUILD SUCCESS`。
- 7 个本批前端文件执行 Prettier 和 ESLint：通过；完整执行 `pnpm --dir jeecgboot-vue3 build`：构建成功。
- 前端完整构建仍会报告项目既有的 `debug`、`chalk`、`clean-css` 解析警告和部分 `:deep` 警告，但未阻止打包，且不来自本批工作流文件。
- 单体启动模块及其 14 个相关模块重新打包成功，最新 JAR 已启动；Flowable `ProcessEngine` 创建成功，JeecgBoot 应用完整启动并持续监听 `8080`。
- 首次启动确实复现并暴露监听器循环依赖；延迟注入修复后重新测试、重新打包和重新启动，循环依赖不再出现。
- 前端开发服务器已启动并监听 `3100`，首页 HTTP 返回 `200`；后端鉴权保护的健康地址返回 `401`，确认请求已到达运行中的 JeecgBoot 安全链。

### 已知差距

- 当前后端仍是 JeecgBoot 身份、权限、租户和响应格式适配后的 Flowable 可移植模块，不是 `yudao-module-bpm` 逐文件同构副本；本批只对齐了核心状态机和分页筛选语义。
- 当前前端菜单信息架构已分为“流程管理”8 项和“审批中心”5 项，但目标 `yudao-ui-admin-vben` 的大量详情、模型配置、多实例、打印和流程图子组件尚未全部迁移，因此页面内容仍可能与目标菜单对应页面不同。
- 启动日志中的 `C:\\config\\jeecg` 代码生成模板目录不存在异常来自项目现有启动监听器；应用已在该异常之后报告启动成功，不影响当前工作流服务，但后续应单独整理启动目录配置。
- 尚未创建用于回归的已发布流程；自然完成监听器已由单元测试覆盖，但完整运行态状态更新仍需通过真实流程实例确认。

### 后续目标

1. 先按目标前端 108 个 Vue 文件建立“菜单 → 页面 → 子组件 → API”差异矩阵，优先补齐模型设计、流程定义、流程实例和任务详情的缺失组件。
2. 在用户确认测试流程摘要后，创建并发布专用回归流程，验证发起、条件预测、自选审批人、审批/拒绝/取消、签名附件和历史时间线的完整状态闭环。
3. 继续迁移会签、或签、多实例完成条件、候选人策略、退回/撤回和子流程语义，并为每批行为增加定向测试。
4. 把 JeecgBoot 用户、角色、租户、文件和权限适配继续收敛到模块边界，形成可独立装配和移植的工作流服务。

## 2026-09-07：修复流程表单设计器布局

### 本次更新

- 新增 `src/plugins/form-create/index.ts`，按目标前端的 `setupFormCreate` 方式注册 FormCreate/FcDesigner 依赖的 Ant Design 组件。
- 在 `main.ts` 启动阶段调用 `setupFormCreate(app)`，解决预编译设计器内部模板无法经过 Vite 自动组件导入的问题。

### 验证结果

- 浏览器重新打开“新增流程表单”后，设计器恢复为左侧工具栏、中间画布、右侧属性栏布局。
- 原先大量 `Failed to resolve component: a-layout-sider / a-form / a-row` 警告已消失，仅剩一个非阻塞的 `columnResize` 事件声明警告。
- 新增插件文件 ESLint 通过；`git diff --check` 未发现空白错误。

### 后续目标

1. 继续补齐表单设计器与目标前端的自定义组件、字段权限和预览行为。
2. 对流程模型设计器及审批详情页做同样的浏览器回归检查。

## 2026-09-07：修复新增流程表单元数据错位

### 本次更新

- 将新增流程表单顶部的“表单名称、状态、备注”从 Ant Design `a-form-item` 改为独立的三列栅格布局。
- 为元数据区域显式设置从左到右的排列和控件自适应宽度，避免 FormCreate/FcDesigner 样式影响外层表单。

### 验证结果

- 浏览器实测：三个字段均已恢复为标签在左、控件在右的同一行布局，输入框不再缩成窄条或上下错位。
- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/form/index.vue`：通过。
- `pnpm --dir jeecgboot-vue3 build`：构建成功；仅保留项目既有的依赖解析和 `:deep` 警告。

## 2026-09-07：统一工作流新增弹窗表单间距

### 本次更新

- 修复“新建流程模型”“新增流程分类”“新增用户组”“新增流程监听器”“新增流程表达式”弹窗因全局 `ant-modal-body` 内边距为 0 导致内容贴边、边界过紧的问题。
- 五个表单统一增加水平和垂直内边距，并将字段底部间距调整为 20px；流程模型表单额外优化栅格行列间距，监听器的双列字段间距调整为 24px。

### 验证结果

- 五个修改页面执行 ESLint：通过。
- `pnpm --dir jeecgboot-vue3 build`：构建成功；仍有项目既有的 `debug`、`chalk`、`clean-css` 解析警告、`minify.compress` 配置警告和 `:deep` 兼容性警告，但未阻止打包。
- 运行中的前端已实测打开“新建流程模型”和“新增流程分类”弹窗，字段与弹窗边界之间已恢复统一留白；其余三个弹窗复用同一间距样式。

## 2026-09-07：修复流程模型分类下拉框为空

### 本次更新

- 为流程模型页面补充 `onMounted(loadModels)`，确保首次进入页面时同步加载流程模型、流程分类和流程表单精简列表；继续保留 `onActivated(loadModels)` 以支持页面重新激活时刷新。
- 原问题是页面首次挂载没有执行数据加载，导致模型编辑弹窗的分类选项仍为空；后端数据库和 `/bpm/category/simple-list` 接口本身已有分类数据。

### 验证结果

- 浏览器重新加载“流程模型”后，已显示现有模型“集成验证流程”。
- 打开“新建流程模型”并展开“流程分类”，已显示分类选项“1”。
- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- 未修改数据库、租户隔离条件或后端权限逻辑。

## 2026-09-07：修复工作流删除接口缺少 id

### 本次更新

- 修复流程模型删除和清理实例接口的 DELETE 请求参数位置：将 `id` 拼接到查询字符串，匹配后端 `@RequestParam String id`。
- 同步修复流程分类、流程表单、用户分组、流程监听器和流程表达式的删除接口，避免相同的 DELETE 参数转换问题。

### 验证结果

- 6 个工作流 API 文件执行 ESLint：通过。
- 使用不存在的模型编号调用 `DELETE /bpm/model/delete?id=missing-test-id`，后端已进入业务校验并返回“流程模型不存在或无权访问”，不再返回 `Required request parameter 'id' ...`。
- 未删除现有流程数据，未修改数据库结构。

## 2026-09-07：按目标稿重构流程模型页面

### 本次更新

- 将流程模型页从卡片网格调整为按流程分类分组的可折叠表格布局，展示流程名称、可见范围、流程类型、表单信息、最后发布和操作列。
- 新增与目标稿一致的顶部工具栏：流程搜索、新建模型、导入和设置入口；分类分组提供折叠、排序和分类管理操作。
- 保留现有模型设计、配置、发布、导出、清理和删除能力，操作菜单改为表格行内操作，继续复用现有接口和权限标识。
- 优化浅灰页面背景、白色内容卡片、分组边界、表头和状态标签，使页面结构与目标截图一致，并兼顾窄屏横向滚动。

### 验证结果

- 浏览器实测 `http://localhost:3100/bpm/manager/model`：已显示“流程模型”工具栏、分类分组表格、流程类型标签、发布时间/版本号和行操作按钮。
- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- `pnpm --dir jeecgboot-vue3 build`：构建成功；仍有项目既有的 `debug`、`chalk`、`clean-css` 解析警告、`minify.compress` 配置警告和 `:deep` 兼容性警告，但未阻止打包。
- 本次只调整流程模型前端页面，没有修改后端接口、数据库结构或权限边界。

## 2026-09-07：修复流程模型保存后图标不更新

### 本次更新

- 流程模型列表的图标原先直接使用接口返回的相对存储路径作为 `img` 地址，浏览器无法访问，导致修改保存后列表仍显示文字占位，视觉上“图标没变”。
- 数据库 `ACT_RE_MODEL.META_INFO_` 中图标字段本身已正确更新（例如 `bpm/model/111_1788768812193.png`），本次未改动保存逻辑。
- 列表头像改为通过 `getFileAccessHttpUrl()` 拼接文件服务完整访问地址后再渲染。

### 验证结果

- 数据库中已保存的图标路径可通过 `http://localhost:8080/jeecg-boot/sys/common/static/bpm/model/111_1788768812193.png` 正常访问（HTTP 200）。
- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- 未修改后端、数据库结构或权限边界。

## 2026-09-07：流程模型分类内拖拽排序

### 本次更新

- 点击流程模型分组右上角“排序”后不弹窗，原列表直接进入排序模式；该分类的操作区切换为“取消/保存排序”。
- 每个流程名称单元格的图标左侧显示拖动把手，拖动表格行可调整该分类内顺序。
- 点击“保存排序”调用 `PUT /bpm/model/update-sort-batch` 保存新顺序并刷新列表；“取消”恢复原顺序。
- 分类下只有一个流程模型时提示无需排序，避免空操作。

### 验证结果

- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- 后端排序接口与现有前端 API 已核对一致，未修改后端、数据库结构或权限边界。

## 2026-09-07：修复排序保存提示缺少 ids 参数

### 本次更新

- 项目 HTTP 封装对 PUT 请求在未传 `data` 时会把 `params` 作为请求体发送，导致 `PUT /bpm/model/update-sort-batch` 的 `ids` 没有进入查询参数，后端报 `Required request parameter 'ids'`。
- 流程模型与流程分类的批量排序请求改为把 `ids` 以多个 `ids=` 查询参数直接拼接到 URL 后发送。

### 验证结果

- `pnpm --dir jeecgboot-vue3 exec eslint src/api/bpm/model/index.ts src/api/bpm/category/index.ts src/views/bpm/model/index.vue`：通过。
- 未修改后端、数据库结构或权限边界。

## 2026-09-08：开发端口调整为 3111 / 8880

### 本次更新

- 为避免本机多套 JeecgBoot 工程占用相同端口，后端从 8080 调整为 8880，前端从 3100 调整为 3111。
- 修改后端 `application-dev.yml` 的 `server.port` 与 `domainUrl.pc`、所有后端 profile 的 PC 前端地址、前端 `.env` 端口、`.env.development`/`.env.production` 的接口地址，以及根目录启动脚本。
- 重新打包后端并在 8880 启动，前端已在 3111 运行。

### 验证结果

- 前端 `http://localhost:3111` 返回 HTTP 200。
- 后端 `http://localhost:8880/jeecg-boot` 及静态资源访问返回 HTTP 200。
- 根目录 `start-dev.bat` 已能在两个新端口上正确识别并跳过重复启动。

## 2026-09-07：修复新建流程模型被当作修改模式

### 本次更新

- 流程模型编辑页复用了同一个响应式表单对象；新建模型时，如果此前打开过某个模型，旧的 `id` 仍残留在表单对象里，导致新页面把流程标识和流程类型按修改模式禁用。
- 补齐空表单的完整字段和默认值，明确重置 `id`、表单、可见范围、流程编码、标题/摘要/打印模板、通知配置等字段；新建时不再继承上一次编辑的内容。

### 验证结果

- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- 未修改后端、数据库结构或权限边界。

## 2026-09-07：流程模型表单信息支持点击预览

### 本次更新

- 流程模型列表中绑定流程表单的“表单信息”名称改为可点击链接。
- 点击后调用流程表单详情接口加载表单字段与布局配置，在弹窗内以只读方式渲染表单预览；无表单的模型仍显示“—”。

### 验证结果

- `pnpm --dir jeecgboot-vue3 exec eslint src/views/bpm/model/index.vue`：通过。
- 未修改后端、数据库结构或权限边界。

## 2026-09-08：简易设计器多人审批接入 Flowable 多实例

### 本次更新

- 简易设计器的“依次审批 / 或签 / 会签”不再只保存界面字段：流程转换器现在生成 Flowable `MultiInstanceLoopCharacteristics`，并写入对应的完成条件。
- 新增统一的多实例候选人集合变量命名约定，由工作流 API 常量提供转换器与运行时引擎共同使用；流程启动和审批推进时会根据候选人策略填充集合，避免 Flowable 在创建任务时拿到空集合。
- 会签新增 1–100% 通过比例配置，并与目标前端的 `approveRatio` 字段兼容；随机一人仍保持普通单实例任务。
- 保留租户和现有候选人策略解析边界，未把工作流运行时重新耦合到芋道 system 模块。

### 验证结果

- 已完成源码级检查，确认转换器、启动流程和完成任务路径使用同一多实例变量契约。
- 按当前要求未执行页面回归、端到端流程测试或审批数据清理；需要重新编译启动后才能在现有环境验证实际任务生成数量。

### 已知差距

- 动态用户组（策略 40）、连续部门负责人及部分候选人策略仍需接入 JeecgBoot 本地用户/部门数据源。
- 多实例拒绝、退回、撤回以及并行分支的完整时间线仍沿用现有简化处理，尚未达到芋道全部运行时语义。

### 后续目标

1. 补齐用户组、部门负责人和流程表达式的运行时候选人解析，并统一租户内用户校验。
2. 完善多实例拒绝/退回/撤回与加签的状态收敛，再推进抄送、触发器和边界事件。
3. 在上述运行时闭环后，再按目标菜单逐页补齐配置面板、签名/打印/报表及独立服务启动器。

## 2026-09-08：补齐流程用户组候选人解析

### 本次更新

- 候选人策略 40（指定用户组）现在由工作流引擎读取 `bpm_user_group`，按当前租户和启用状态展开用户组成员，再提供给多实例任务集合。
- 用户组查询仅选择当前租户的启用用户组，避免跨租户把成员带入流程；非法或不存在的用户组编号不会扩大查询范围。
- 本次继续保持工作流自有用户组数据模型，不引入芋道 system 依赖。

### 验证结果

- 后端工作流模块已重新编译并重新打包启动，Maven `package -DskipTests` 构建成功；未执行测试和页面回归。
- 8880 后端已使用新 JAR 启动，数据库未修改。

### 后续目标

1. 继续接入连续部门负责人、表达式和动态候选人策略，并补充空候选人的可解释错误信息。
2. 收敛会签/或签拒绝、退回、撤回和加签的 Flowable 状态语义。

## 2026-09-08：补齐候选人策略与连续部门负责人规则

### 本次更新

- 新增统一的 `WorkflowCandidateResolver`，按芋道 `BpmTaskCandidateStrategyEnum` 对齐并集中处理策略 10、20、21、22、23、30、34、35、36、37、38、40、50、51、60。
- 修正策略 37“发起人部门负责人”：参数现在表示指定层级，只返回该层负责人；新增策略 38，按 1 到指定层级依次收人并保持顺序去重。
- 新增策略 23“连续多级部门负责人”，兼容芋道的 `部门编号1,部门编号2|层级` 参数格式；每个部门分别向上查找，层级超过组织树高度时停在最高可用部门。
- 新增表单候选人策略：策略 50 从表单用户字段读取单值、数组、集合或逗号分隔值；策略 51 按 `表单部门字段|层级` 读取部门并计算连续多级负责人。
- 新增流程表达式策略 60 的运行时求值；流程预测时若表达式依赖尚不存在的变量或 `execution`，返回空候选人而不阻断页面。
- “随机一人”不再把岗位编号、用户组编号误当作 Flowable 候选组。转换后的普通单实例任务通过 `${workflowCandidateResolver.resolveOne(execution)}` 在任务创建时计算全部候选人并随机确定负责人。
- 更新简易模型转换器的既有测试契约：随机一人节点应生成运行时负责人表达式，不再生成可认领的静态候选用户列表（本次未执行测试）。
- 所有候选结果统一过滤不存在、删除或冻结的用户，避免生成永远无法办理的任务。
- 新增 `WorkflowCandidateIdentityAdapter` 及 JeecgBoot 本地实现，把角色、部门、负责人、岗位、主部门和父部门查询从流程算法中隔离；后续独立服务可替换为远程身份适配器。
- 简易设计器候选人下拉补齐部门成员、部门负责人、连续多级部门负责人、岗位、发起人部门负责人、发起人连续多级部门负责人、表单用户字段和表单部门负责人；角色、部门、岗位改用现有 JeecgBoot 选择组件，层级参数按目标格式保存。

### 与目标实现的对应关系

- 10 角色、20 部门成员、21 部门负责人、22 岗位、30 用户、34 审批人自选、35 发起人自选、36 发起人、40 用户组：已接入统一解析器。
- 23 指定部门连续负责人、37 发起人指定层负责人、38 发起人连续负责人、50 表单用户、51 表单部门负责人、60 表达式：本次新增运行时解析。
- JeecgBoot 用户允许属于多个部门；为匹配芋道单一主部门语义，策略 37/38 优先使用用户 `orgId`，其次使用 `orgCode`，最后回退到用户所属部门列表的第一项。

### 验证说明

- 按用户要求，本次未执行 Maven/前端编译、单元测试、页面回归、流程启动或数据库操作。
- 后续人工验证可新建同一流程的随机一人、会签、或签和依次审批节点，分别选择上述策略，重新发布后检查任务负责人和任务数量。

### 已知差距

- 审批人为空时的自动通过、自动拒绝、指定人员和流程管理员兜底尚未移植；当前动态策略解析为空时，随机单实例任务会保持未分配。
- 表单字段选择目前使用字段名称输入，尚未根据所绑定表单动态展示可选字段；目标版的完整选择面板仍在后续设计器配置任务中。
- 多实例拒绝后的实例撤销、循环退回重新计算候选人、加签和并行时间线仍需继续对齐芋道自定义 Flowable Behavior。

### 后续目标

1. 移植审批人为空与审批人等于发起人的处理规则，避免空候选任务停滞，并补齐流程管理员兜底。
2. 对齐芋道并行/串行多实例 Behavior，完善拒绝、退回、撤回、加签和重新进入节点时的候选人重算。
3. 继续完成超时/边界事件、抄送与触发器运行语义，再推进完整配置面板和独立工作流启动器。

## 2026-09-09：补齐空审批人与发起人重合处理规则

### 本次更新

- 简易流程转换器现在把 `assignEmptyHandler` 和 `assignStartUserHandlerType` 写入 Flowable BPMN 扩展元素，字段名及枚举值与芋道保持一致。
- “审批人为空”已接入四种运行时语义：自动通过、自动拒绝、指定人员审批、转交给流程管理员。指定人员从节点配置读取；流程管理员从当前流程定义对应模型的 `managerUserIds` 读取，并按租户定位模型。
- 候选人解析完成后，会先过滤冻结或删除的普通候选人，再执行空审批人兜底；兜底人员不再次过滤，避免兜底后再次变成无人审批，与目标实现的处理顺序一致。
- “审批人与发起人相同”已接入三种语义：本人审批、自动跳过、转交发起人主部门负责人。自动跳过在存在多个候选人时先排除发起人；只有发起人一人时创建任务后自动通过。
- 新增任务创建/分配事件监听器。自动审批动作延迟到当前事务完成后执行，避免在 Flowable 尚未持久化任务时递归完成任务；自动处理会写入任务结果和流程评论。
- 空候选人的多实例节点现在生成一个空办理人实例，由事件监听器执行自动通过或自动拒绝，避免 Flowable 因空集合直接跳过节点。
- 退回到历史节点时写入节点级退回标记，重新进入该节点不会因“审批人与发起人相同”再次自动跳过；人工完成后清理该标记。
- 简易设计器节点面板新增上述四种空审批人规则、指定兜底人员选择，以及三种发起人重合规则。
- 更新转换器测试契约，增加三个 BPMN 扩展字段的断言；本次遵照当前要求未执行测试。

### 验证说明

- 本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作。
- 后续人工验证时需重新编译并重启后端、重新保存并发布简易流程模型；旧版本流程定义不会自动获得新扩展配置。
- 建议分别创建五条最小流程验证：空审批人自动通过、空审批人自动拒绝、指定人员兜底、流程管理员兜底、发起人相同自动跳过/转部门负责人。

### 已知差距与风险

- 芋道为并行、串行多实例替换了自定义 Flowable Behavior；当前实现完成了空集合保留任务与基础完成条件，但多实例拒绝后的兄弟实例取消、串行动态加减签及历史状态收敛仍未完全对齐。
- BPMN 设计器的完整自定义属性面板尚未移植，本次配置入口位于简易设计器；手工导入 BPMN 时可识别同名扩展元素，但现有 BPMN 设计器尚不能完整编辑它们。
- 转交部门负责人当前沿用 JeecgBoot 主部门适配规则；用户多部门且未设置主部门时会回退到所属部门第一项。
- 事件监听器对自动处理异常采用记录日志并保留任务的策略，避免流程事务被异步规则反向回滚；仍需在端到端验收中检查异常告警与人工补偿入口。

### 后续目标

1. 对齐芋道并行/串行多实例 Behavior，补齐或签完成后的兄弟任务取消、会签拒绝阈值、动态加减签和历史状态。
2. 完善退回、撤回、跳过表达式、超时与边界事件，并覆盖循环退回后的候选人重新计算。
3. 继续移植 BPMN 设计器完整节点配置面板，再实现抄送、触发器、字段/按钮权限与监听器运行闭环。
4. 推进签名、打印模板、流程报表、独立工作流启动器及菜单页面逐项验收。

## 2026-09-09：多实例候选人重算与取消状态收敛

### 本次更新

- 新生成的会签、或签和依次审批节点不再只依赖流程启动时预填的候选人集合，改为通过 `${workflowCandidateResolver.resolveAll(execution)}` 在每次进入节点时重新计算候选人。
- 运行时重算覆盖首次进入、退回后重新进入和循环流转，能够读取当时的表单变量、发起人自选/审批人自选结果以及最新组织人员关系；原有集合变量初始化逻辑暂时保留，用于兼容已经发布的旧流程定义。
- `resolveAll` 保持候选人顺序，并在最终候选为空时返回一个空元素，使 Flowable 一定创建任务，再交由上一轮新增的空审批人规则自动通过或自动拒绝。
- 任务事件监听器新增 `ACTIVITY_CANCELLED` 处理：或签一人完成、多实例提前结束、拒绝跳转结束以及其它流程跳转取消活动时，在活动删除前把尚未结束的任务标记为“已取消”并记录原因。
- 退回操作在活动跳转前把当前任务标记为“已退回”，避免取消事件把退回历史错误覆盖为普通取消。
- 历史任务接口根据 `TASK_STATUS` 区分已通过、未通过、已取消、已退回和通过中；流程详情时间线同步展示这些精确状态，不再把所有带 `deleteReason` 的历史任务都显示为“已取消”。
- 自动拒绝和多人任务取消改用任务自身租户编号查询活动任务，避免事务后自动处理依赖当前登录上下文。

### 验证说明

- 本次遵照当前要求，未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作。
- 后续验证需要重新编译后端，重新保存并发布一个包含会签、或签、依次审批及循环退回的流程；旧定义仍使用旧集合表达式，但保留的兼容初始化逻辑可以继续运行。
- 建议观察流程详情时间线：或签剩余任务应为“已取消”，退回的原任务应为“已退回”，重新进入节点后应按最新候选人重新生成任务。

### 已知差距与风险

- 当前会签按目标现有表达式使用“已完成实例数/总实例数”计算比例；拒绝仍按系统级拒绝直接结束流程，尚未实现按拒绝比例推导会签结果的扩展策略。
- 串行多实例的动态加签/减签、运行中修改实例总数，以及向前/向后加签的 WAIT、APPROVING 状态转换仍需继续对齐。
- 并行分支与多实例叠加时，强制跳转结束后的残留 execution 兜底清理尚未移植；复杂嵌套流程仍需专项处理。
- 本轮只完善运行时和详情状态展示，BPMN 设计器的多人审批完整面板仍未完成。

### 后续目标

1. 移植多人审批的加签/减签和 WAIT、APPROVING 状态机，并处理串行执行顺序与实例计数更新。
2. 完善并行分支、多实例拒绝及撤回时的 execution 收敛和历史时间线。
3. 衔接跳过表达式、超时/边界事件，再推进抄送、触发器和节点权限/监听器闭环。
4. 继续补齐 BPMN 设计器配置面板、签名打印报表、独立启动器和菜单端到端验收。

## 2026-09-09：对齐前加签、后加签与减签状态机

### 本次更新

- 按芋道 `BpmTaskSignTypeEnum` 和 `BpmTaskStatusEnum` 对齐前加签、后加签的运行时语义，并新增任务状态 `WAIT = 0`、`RUNNING = 1`；加签类型改为写入 Flowable 任务 `scopeType`，同时兼容此前以 `workflow-sign:*` 写入 `category` 的运行中任务。
- 前加签时，原审批任务保存原审批人为 `owner`、清空 `assignee` 并进入 `WAIT`；新建的加签子任务立即进入 `RUNNING`。全部子任务处理完后，原任务恢复给 `owner` 并重新进入 `RUNNING`，不会和前加签人同时审批。
- 后加签时，新建子任务只保存 `owner` 并进入 `WAIT`。原审批人点击通过后，原任务不立即完成，而是进入 `APPROVING`，保存审批意见、签名、附件和下一节点变量，再激活全部后加签子任务；最后一个子任务通过后才完成原任务并推动流程继续。
- `APPROVING` 父任务在后加签处理期间拒绝重复通过或拒绝，详情页隐藏普通审批按钮并显示“等待后加签任务完成”的状态提示，避免流程被重复推进或反向结束。
- 加签子任务改为按芋道字段集合复制父任务的流程实例、流程定义、任务定义、优先级、租户等信息，不再创建缺少流程实例关联的孤立任务。
- 同一节点支持继续追加同类型加签；前加签和后加签不能混用，并阻止重复选择当前节点已有审批人或加签人。已进入 `APPROVING` 的任务不能继续增加加签人。
- 减签会递归取消被选任务及其下级加签任务，先写入“已取消”状态和原因，再删除运行任务；删除最后一个前加签任务时恢复原审批任务，删除最后一个尚未开始的后加签任务时保留原任务继续审批，只有原任务已经处于 `APPROVING` 时才自动完成原任务。
- 审批详情的待办任务新增 `children` 子任务数据；右侧增加紧凑的“加签任务”列表及减签入口，显示处理人和 WAIT/RUNNING/APPROVING 状态，没有调整基础表单或流程图布局。
- 加签子任务不再预测或要求选择 BPMN 下一节点审批人，并显式禁止执行退回；子任务完成只用于收敛其加签父任务，避免使用不存在的 execution 发起流程跳转。
- 加签任务 `owner` 现在属于合法流程参与者，可在父任务 WAIT/APPROVING 阶段继续查看流程详情、评论和子任务；这与候选审批人查看权限分开，不会让普通 owner 绕过实际审批任务校验。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启、数据库查询或数据修改；仅进行了目标源码对照和当前源码静态复核。
- 后续人工验证需要重新编译并重启前后端，然后分别建立两条最小流程：
  1. 前加签：A 前加签给 B、C，确认 A 进入等待且不能审批；B、C 全部通过后，A 恢复为可审批。
  2. 后加签：A 后加签给 B、C，确认 B、C 初始不可审批；A 通过后状态显示“通过中”，B、C 出现在各自待办；两人全部通过后才进入下一节点。
  3. 分别在子任务未开始、部分完成和父任务已进入 `APPROVING` 时执行减签，确认父任务恢复或完成条件符合上述规则，并检查时间线中的取消状态。

### 已知差距与风险

- 本轮未进行编译与真实 Flowable 数据验证，因此 `TaskEntityImpl` 子任务持久化、事务内最后一个子任务查询以及历史变量落库仍需后续按上述场景验收后才能视为已验证。
- 当前补齐的是任务级前/后加签；会签、或签多实例执行体运行中动态加减实例、串行插入顺序与实例计数仍未达到芋道自定义 Behavior 的完整能力。
- 加签列表当前显示用户编号，尚未像目标版一样补充用户昵称、头像和部门信息；这不影响流转，但属于界面信息完整性差距。
- 复杂并行分支中拒绝或撤回时的 execution 收敛、加签任务与边界事件组合仍未完成专项处理。

### 后续目标

1. 继续对齐会签、或签的自定义 Flowable Behavior，补齐运行中动态加减实例、串行顺序、实例计数和拒绝阈值。
2. 完善并行分支、多实例拒绝、退回和撤回的 execution 收敛及完整历史时间线。
3. 实现跳过表达式、超时与边界事件，再补齐抄送节点、触发器节点和节点字段/按钮/监听器运行闭环。
4. 继续移植简单设计器及 BPMN 设计器完整配置面板，随后完成签名、打印模板、流程报表、独立工作流启动器和菜单页面端到端验收。

## 2026-09-09：补齐节点“拒绝后处理”与多实例退回收敛

### 本次更新

- 新增与芋道 `BpmUserTaskRejectHandlerTypeEnum` 一致的节点拒绝处理配置：`rejectHandlerType = 1` 表示直接终止流程，`rejectHandlerType = 2` 表示驳回到指定任务节点，并配套 `rejectReturnTaskId` 扩展元素。
- 简易流程转换器现在把每个审批/办理节点的 `rejectHandler` 写入 Flowable BPMN；未配置时默认“终止流程”，配置为驳回但缺少目标节点时会在保存阶段给出明确错误。
- 审批拒绝时引擎先读取当前节点的拒绝处理配置：配置为驳回时不再把流程实例标记为最终“未通过”，而是按目标节点执行退回；未配置或配置为终止时维持原有拒绝终止流程行为。
- 手工“退回”与“拒绝后驳回”统一走同一套退回逻辑，按目标节点从 BPMN 结构向后查找应撤销的活动任务；会签/或签同一节点的多个实例会一起撤回到目标节点，而不是只移动当前一个 execution。
- 退回前会先标记当前任务为“已退回”、可达路径上的其余活动任务为“已取消”，并设置目标节点退回标记，使重新进入节点时不会错误执行“审批人等于发起人自动跳过”等规则。
- `WorkflowBpmnNavigator` 新增按 BPMN 结构（不依赖条件表达式值）查找目标节点之后运行任务的方法，避免流程变量在退回时变化导致漏选分支任务。
- 简易设计器审批/办理节点新增“拒绝后处理”配置区：可选择直接终止或驳回，并选择流程中已有的发起人/审批/办理节点作为退回目标；默认配置与现有模型兼容。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启、数据库查询或数据修改；仅进行了目标源码对照与当前源码静态复核。
- 后续人工验证需要重新编译并重启前后端，然后重新保存并发布流程，再分别验证：
  1. 节点“拒绝后处理=终止流程”：拒绝后流程实例显示未通过并结束，与之前一致。
  2. 节点“拒绝后处理=驳回到指定节点”：选择上游审批/发起人节点，拒绝后应回到该节点并重新生成待办，流程实例状态保持运行中。
  3. 或签/会签节点执行驳回：同节点其余未办理任务应显示为已取消，不残留待办。

### 已知差距与风险

- 本轮只对齐“拒绝终止/驳回”的运行时语义；芋道还允许驳回策略作为审批按钮触发，并支持在配置时精确选择目标节点，当前界面提供全流程节点下拉并由运行时校验可达性，尚未做到与目标版完全一致的节点选择交互。
- 多实例退回虽然改为按活动节点集合跳转，但复杂并行分支、嵌套子流程和循环退回仍缺少端到端验证；退回后重新进入会签节点时是否重新生成正确数量的任务也需按上述场景实测。
- 撤回、跳过、超时/边界事件仍沿用现有实现，尚未完成专项对齐。

### 后续目标

1. 继续对齐并行分支、子流程和循环退回下的多实例 execution 收敛，并覆盖撤回后重新进入节点。
2. 实现跳过表达式、超时与边界事件，再补齐抄送节点、触发器节点和字段/按钮/监听器运行闭环。
3. 继续移植简单设计器及 BPMN 设计器完整配置面板，随后完成签名、打印模板、流程报表、独立工作流启动器和菜单页面端到端验收。

## 2026-09-09：补齐简易设计器“抄送节点”运行时语义

### 本次更新

- 简易流程中的“抄送节点”不再被转换为空服务任务，而是生成指向 `workflowCopyTaskDelegate` 的 Flowable 委托服务任务，与芋道 `BpmCopyTaskDelegate` 的结构一致。
- 转换器会把抄送节点的 `candidateStrategy`、`candidateParam` 写入 BPMN 扩展元素，使流程进入该节点时能按当前候选人配置计算抄送人。
- 新增 `WorkflowCandidateResolver.resolveCopyUsers(...)`，支持角色、部门、负责人、岗位、用户、用户组、发起人/自选审批人、表单用户字段和流程表达式等策略，但不应用“空候选人自动处理”，抄送人为空时直接跳过，避免抄送节点产生审批动作。
- 新增 `WorkflowCopyNodeDelegate` JavaDelegate：流程到达抄送节点时，为解析出的每个用户写入一条 `bpm_process_instance_copy` 抄送记录，并携带当前活动节点名称和“抄送节点”原因；同一用户在同一流程实例同一活动节点不会重复抄送。
- `WorkflowProcessCopyService` 新增节点抄送写入方法，不再依赖当前登录用户；流程执行没有 Shiro 登录上下文时也能按执行租户落库。
- 现有审批详情“抄送”按钮及其手动抄送逻辑保持原样，本次只补齐流程节点自动抄送的运行时。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了目标源码对照与当前源码静态检查。
- 后续人工验证需要重新编译并重启后端，重新保存并发布包含抄送节点的流程，观察流程流转经过抄送节点后，抄送人是否能在“抄送给我的”列表看到记录，且不产生待办任务。
- 建议分别验证指定用户、指定角色、发起人本人和自选审批人几种抄送候选人策略。

### 已知差距与风险

- 触发器节点目前仍是空服务任务，尚未实现 HTTP 请求/回调、表单数据更新等实际语义；本轮没有扩大改动范围。
- 抄送页面的展示仍依赖现有流程抄送列表接口；如果后续需要与芋道一样展示抄送活动节点名称，仍需做前端对齐。
- JavaDelegate 委托表达式依赖 Spring 表达式解析 Flowable bean；当前代码已按芋道相同写法实现，但实际解析仍需要在编译启动后的端到端流程中验证。

### 后续目标

1. 实现触发器节点类型分发，并优先接入 HTTP 请求与回调的基础运行时语义。
2. 继续处理超时/边界事件、跳过表达式和节点监听器闭环。
3. 继续移植并行分支、子流程、循环退回及完整时间线，随后推进简单设计器/BPMN 设计器完整配置面板、签名、打印报表、独立启动器和菜单端到端验收。

## 2026-09-09：补齐触发器节点运行时委托（HTTP 请求、流程表单更新/删除）

### 本次更新

- 简易流程“触发器节点”不再生成空服务任务，改为指向 `workflowTriggerTaskDelegate` 的 Flowable 委托服务任务。
- 转换器将触发器的 `triggerType` 和序列化后的 `triggerParam` 写入 BPMN 扩展元素；触发器参数只保存 http/form 相关设置，不与流程模型耦合。
- 新增 `WorkflowTriggerNodeDelegate`：
  - 类型 1（HTTP 请求）：按节点保存的 URL 与请求参数发起同步 POST，自动携带 `processInstanceId`，支持固定值与流程变量值；失败时抛出业务异常，避免流程在未完成请求的情况下继续。
  - 类型 10（更新流程表单）：解析 `updateFormFields`，把结果合并进流程实例变量。
  - 类型 11（删除流程表单）：解析 `deleteFields`，从流程实例变量中移除对应字段。
  - 类型 2（HTTP 回调）：暂保持兼容通过，不阻断旧模型，等待后续接入附加 ReceiveTask 与回调接口。
- 表单更新/删除只允许修改普通表单变量，`PROCESS_`、`TASK_`、`_workflow` 前缀变量不会被触发器覆盖，避免破坏流程内部状态。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了目标源码对照与当前源码静态检查。
- 后续人工验证需要重新编译并重启后端，重新保存并发布触发器节点流程：
  1. HTTP 请求触发器配置一个可访问的本地接收地址，确认流程到达节点时收到 `processInstanceId`，调用失败时流程会报错而不是继续。
  2. 流程表单更新触发器确认流程变量被写入，删除触发器确认字段被移除。

### 已知差距与风险

- HTTP 回调（类型 2）尚未实现真正的“流程等待业务回调”，仍需在转换器中生成 ReceiveTask、在流程详情提供回调地址并接入触发/接收接口。
- 简易设计器目前只暴露 URL 配置，尚未补齐目标版完整的请求头、请求体、返回字段映射和表单触发器可视化编辑；本次先做后端运行时契约。
- HTTP 请求由低代码流程模型配置 URL，属于设计时可信输入；正式接入时仍需校验 URL 协议并考虑内网地址与超时限制。

### 后续目标

1. 接入 HTTP 回调的 ReceiveTask、回调参数与续跑接口，完成触发器全类型运行语义。
2. 完善触发器、抄送、字段权限和按钮权限的简易设计器配置面板。
3. 继续处理超时/边界事件、跳过表达式、子流程/并行时间线，随后推进签名、打印、报表、独立启动器和端到端验收。

## 2026-09-09：补齐节点跳过表达式运行时

### 本次更新

- 简易流程的审批、办理、抄送和触发器节点现在可以把 `skipExpression` 写入 Flowable Activity 的跳过表达式属性；用户填写 `amount > 1000` 会自动补成 `${amount > 1000}`。
- 发起流程时默认写入 `_FLOWABLE_SKIP_EXPRESSION_ENABLED = true`，使 Flowable 在表达式满足时直接跳过节点，不生成待办任务。
- 简易设计器审批/办理节点新增“跳过设置”，可填写跳过表达式；抄送、触发器节点通过模型 JSON 中的同名字段兼容。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了目标源码对照与当前源码静态检查。
- 后续人工验证需要重新编译并重启后端，重新保存并发布含跳过表达式的流程，建议验证：
  1. 节点填写 `${total >= 1000}` 且流程变量满足时，该节点不产生待办，直接进入下一节点。
  2. 表达式不满足时，节点仍正常生成待办。

### 已知差距与风险

- 被 Flowable 自动跳过的节点目前没有独立的历史任务记录，审批时间线仍需要进一步把跳过活动识别为“已跳过”状态展示。
- 退回/驳回后若目标节点再次满足跳过表达式，Flowable 会按表达式继续跳过；如果需要“退回后必须人工审批一次”的语义，后续要在退回标记与跳过引擎之间加额外判断。

### 后续目标

1. 补齐时间线对跳过活动的展示，并完善退回后跳过表达式行为。
2. 实现超时/边界事件、子流程与并行分支的时间线收敛。
3. 继续推进配置面板、字段/按钮权限、签名、打印报表、独立启动器和菜单端到端验收。

## 2026-09-09：审批时间线识别“已跳过”节点

### 本次更新

- 新增跳过任务状态常量 `TASK_STATUS_SKIPPED = -2`，与目标前端时间线已有的“已跳过”展示值保持一致。
- `WorkflowBpmnNavigator` 新增跳过表达式求值入口，供详情时间线复用运行时同一表达式语义。
- 审批详情构建时间线时，如果节点没有待办/已办任务、节点配置了跳过表达式且表达式成立，会把该节点显示为“已跳过”，而不是“未开始”。
- 为减少误判，只有该节点之后已有实际任务，或流程已经正常结束时才标记“已跳过”；流程被拒绝、取消或仍在较早节点等待时，后续未到达节点仍显示“未开始”。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了静态源码检查和 `git diff --check`。
- 后续人工验证需要重新编译并重启后端，发布一条“A 满足跳过、B 正常审批”的流程，确认 A 显示“已跳过”、B 正常生成待办，且审批时间线能看出 A 已经过。

### 已知差距与风险

- 当前“已跳过”是基于流程变量、后续任务与结束状态的推断，尚未读取 Flowable 跳过的历史活动实例；复杂并行分支中某个分支未执行但同层后续任务存在时，仍可能出现保守或偏宽的展示。
- 跳过节点没有开始/结束时间展示。

### 后续目标

1. 改为基于 Flowable 历史活动实例识别跳过节点，并补充开始/结束时间。
2. 实现超时与边界事件、撤回后的 execution 收敛。
3. 继续推进配置面板、字段/按钮权限、监听器闭环、签名、打印报表、独立启动器和端到端验收。

## 2026-09-09：审批节点操作权限与签名/意见必填闭环

### 本次更新

- 简易设计器审批/办理节点现在会把 `approveType`、`enabledButtons`、`reasonRequire`、`signEnable` 写入 BPMN 扩展元素；前端新增“审批签名”开关，旧模型默认不开启签名。
- 审批详情接口根据当前待办任务所在节点返回可用按钮列表、是否必填审批意见、是否必须上传签名。
- 审批详情页按节点配置显示/隐藏“通过、拒绝、退回、转办、委派、加签、抄送”按钮；隐藏了原按钮后审批操作区不再展示不可用操作。
- 提交审批时前端与后端同时校验：
  - 节点配置 `reasonRequire` 时，通过操作必须填写审批意见；
  - 节点配置 `signEnable` 时，通过操作必须上传审批签名。
- 后端校验位于真实完成任务的入口，不能只靠前端隐藏按钮绕过；自动完成、空人自动处理等系统路径不受签名/意见必填约束。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了目标源码对照和 `git diff --check`。
- 后续人工验证需要重新编译并重启前后端，重新保存并发布一个审批节点，分别关闭/开启部分按钮、审批意见必填、签名必填，确认页面按钮、校验和直接调接口绕过均符合预期。

### 已知差距与风险

- 表单字段权限（`formFieldsPermission`）仍是空集合，尚未按节点字段权限配置对表单做只读/可编辑控制；按钮与字段权限属于同一目标项，字段权限部分仍需继续。
- 办理节点（类型 13）与审批节点共用按钮配置，但页面操作区当前主要面向审批语义，后续需按节点类型微调文案与按钮。

### 后续目标

1. 补齐字段权限在审批详情/发起页面的读写控制，并让表单按节点配置动态禁用字段。
2. 实现节点监听器运行闭环，再处理超时/边界事件。
3. 继续推进配置面板、签名打印、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：补齐节点表单字段权限运行闭环

### 本次更新

- 简易流程审批/办理节点支持 `fieldsPermission`：既可以是 `[{field, permission}]` 数组，也可以直接粘贴 JSON 字符串；转换器统一规范为 `READ/WRITE/NONE` 并写入 BPMN `formFieldsPermission` 扩展元素。
- 审批详情接口读取当前待办节点权限配置并返回 `formFieldsPermission`，字段映射格式与目标前端一致。
- 审批详情页根据字段权限渲染：
  - `READ` 或未配置：字段只读；
  - `WRITE`：字段可编辑，表单整体不再全局禁用；
  - `NONE`：不展示该字段。
- 发起流程和审批页面共用现有 form-create 渲染逻辑；只读字段仍保留在原表单结构中，不影响已提交流程变量。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了静态源码检查和 `git diff --check`。
- 后续人工验证需要重新编译并重启前后端，重新保存并发布节点，并在“字段权限 JSON”中配置一个 `WRITE` 字段、一个 `READ` 字段，确认审批页面只有前者可编辑。

### 已知差距与风险

- 字段权限目前采用 JSON 文本配置，尚未像目标版一样绑定流程表单字段后提供可视化的字段选择/权限下拉；完整的字段权限设计器属于后续配置面板任务。
- 字段隐藏（`NONE`）只影响展示，若表单引擎仍回传该字段值，后端没有按节点过滤变量；需要把字段写入校验和表单变量过滤再纳入后续闭环。

### 后续目标

1. 让设计器根据已关联表单动态展示字段权限可视化配置，并补充字段值写入过滤。
2. 实现节点监听器运行闭环，再处理超时/边界事件。
3. 继续推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：子流程输入/输出变量传递

### 本次更新

- 简易设计器子流程节点新增“输入变量 JSON / 输出变量 JSON”，例如：
  - 输入：`[{"source":"applyUserId","target":"applyUserId"}]`
  - 输出：`[{"source":"result","target":"subResult"}]`
- 转换器为 CallActivity 生成 `flowable:in/out` 参数，主流程变量在进入子流程前传递，子流程结束时把结果变量回写到主流程。
- 无论模型是否配置，转换器都会自动补充主流程→子流程的 `PROCESS_STATUS`、`_workflowStartUserId`、`_workflowTenantId`，避免子流程发起人/状态语义丢失。
- 后端支持 JSON 数组字符串或原生数组两种模型写法，兼容目标版 simple model 结构。

### 验证说明

- 已重新编译打包后端并重启，后端在 8880 可访问，HTTP 探活返回 302（Spring 登录跳转）。
- 未执行单元测试和端到端流程验证；下一步可用“主流程表单字段→子流程节点→主流程后续节点”验证变量是否真实传递与回写。

### 已知差距与风险

- 子流程异步启动、发起人配置、超时和多实例子流程仍只保留基础结构，尚未完整移植。
- 输出变量回写依赖 Flowable CallActivity 执行完成；若子流程异常终止，主流程回写不会发生。

### 后续目标

1. 补齐子流程发起人配置、异步启动、超时和多实例来源。
2. 完善并行分支、循环退回、撤回的 execution 收敛。
3. 继续推进 HTTP 回调、打印报表、独立启动器和菜单端到端验收。

## 2026-09-09：子流程异步启动与实例名

### 本次更新

- 后端现在把子流程节点的 `async` 写入 CallActivity，异步子流程不再只是前端开关无效。
- 支持 `calledProcessDefinitionName`，可设置子流程实例名称表达式；前端子流程节点新增“子流程实例名”输入框。
- 与目标版一致：异步子流程不生成 `flowable:out` 回写参数，避免异步结束后写回主流程导致状态不一致。

### 验证说明

- 本轮已完成后端编译打包并重启，8880 可访问；未执行子流程端到端流程验证。
- 后续人工验证：主流程→子流程节点，勾选异步启动后确认子流程能正常创建并继续推进。

### 已知差距与风险

- 子流程发起人配置（从发起人/字段/部门推导）仍未移植。
- 异步子流程的异常处理、超时和状态回写仍需专项验证。

### 后续目标

1. 补齐子流程发起人配置与超时边界事件。
2. 继续完善 HTTP 回调、多实例 execution 收敛。
3. 推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：流程详情打印样式收口

### 本次更新

- 流程详情页原有“打印”走 `window.print()`，本次把打印样式收口：打印时隐藏页面返回按钮、右上角操作按钮、审批操作卡和评论输入按钮。
- 同时隐藏底部“查看 BPMN XML”折叠源码，避免把 XML 大段文本带入打印稿。
- 保留申请信息、表单数据、审批时间线和流程节点条，适合作为流程档案纸质留存。

### 验证说明

- 该改动为纯前端样式改动，前端 dev 已运行，热更新后可直接点击详情页“打印”查看打印预览。
- 未重新打包后端，不影响当前使用。

### 已知差距与风险

- 当前仍是浏览器打印当前页面，没有独立的打印模板路由；目标版更完整的模板打印需后续单独实现。
- 表单为 form-create 渲染，若字段较多仍可能出现跨页断行，打印模板需要继续优化分页。

### 后续目标

1. 实现独立打印模板页面，并支持按表单字段排版分页。
2. 继续完善多实例 execution 收敛与字段权限。
3. 推进流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：审批时间线显示任务处理人姓名

### 本次更新

- `WorkflowTaskVO` 新增 `assigneeName`、`ownerName`，待办/已办/历史任务和加签子任务都会返回处理人姓名。
- 流程详情时间线和“加签任务”列表优先显示姓名，只有无法解析用户时才回退显示用户编号或“待认领”。
- 这样发起人和审批人不再直接暴露一串用户 UUID，页面更接近目标版展示。

### 验证说明

- 本轮为后端字段+前端展示改动，尚未打包；前端 dev 热更新后需等后端下轮统一打包才能看到姓名。
- 后续建议检查历史任务、加签任务、退回任务是否都显示真实姓名。

### 已知差距与风险

- 批量任务转换会逐个查询用户姓名，流程实例任务较多时可能产生额外查询；如后续影响性能可改为批量用户查询。

### 后续目标

1. 继续完善多实例 execution 收敛与字段权限。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：发起人/管理员取消流程时显式标记任务取消

### 本次更新

- 取消运行中流程前，先查询流程实例内所有活动任务并写入“已取消”状态和取消原因，再删除流程实例。
- 这样取消流程后，并行分支、会签兄弟任务和加签任务不会在历史详情中残留为“审批中”。
- 发起人取消和管理员取消共用同一段收敛逻辑。

### 验证说明

- 本轮后端改动尚未打包；等统一打包后建议构造并行流程后取消，确认所有活动任务历史状态为已取消。

### 后续目标

1. 继续完善多实例 execution 收敛。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：统一打包并重启验证

### 本次更新

- 按用户确认将此前积累的后端改动统一编译打包：`jeecg-system-start-3.9.5.jar` 已重新生成。
- 后端已重启并通过 HTTP 探活，8880 返回登录跳转；前端 3111 已重新启动并返回 200。
- 打包内容包括：任务姓名展示、撤回结构可达、取消/拒绝任务状态收敛、字段权限后端过滤、删除已结束流程实例、取消流程评论、跳过预览等。

### 当前验证状态

- 已执行：Maven `package -DskipTests` 成功、服务启动成功、HTTP 探活成功。
- 未执行：完整端到端业务回归、页面逐菜单验收；需要在浏览器中按功能继续人工验证。

### 后续目标

1. 按页面逐项验收本轮功能，优先验证撤回、删除历史流程、任务姓名与字段权限。
2. 继续推进多实例 execution 收敛、子流程发起人配置、流程报表。

## 2026-09-09：管理员删除已结束流程实例

### 本次更新

- 新增管理接口 `DELETE /bpm/process-instance/delete?id=xxx`，只允许删除已结束或已取消的流程实例。
- 删除会同步清理 Flowable 历史流程、历史任务变量、评论等历史数据，并删除 `bpm_process_instance_copy` 抄送记录。
- 流程实例管理页对非运行中记录显示“删除”操作，带二次确认；运行中记录仍只能“终止”。

### 验证说明

- 本轮为前后端改动，尚未打包；待统一打包后可在流程实例管理页对已结束流程执行删除。

### 后续目标

1. 继续完善多实例 execution 收敛。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：取消流程写入流程级取消评论

### 本次更新

- 发起人或管理员取消流程时，在删除流程实例前写入一条 `cancel` 类型的流程评论，内容为取消原因。
- 流程详情“流程评论”时间线会显示“取消”，而不是只看到流程状态变化。
- 前端评论类型映射补充“取消”，热更新即可展示。

### 后续目标

1. 继续完善多实例 execution 收敛。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：发起前审批预览显示“将自动跳过”节点

### 本次更新

- 发起流程前调用审批预览时，后端会根据当前已填写的流程变量判断后续节点是否满足跳过表达式。
- 满足跳过的节点在返回的审批路径中标记为“已跳过（-2）”，不再一律显示“未开始”。
- 发起页右侧审批预览与流程图预览会提示“该节点满足跳过条件，将自动跳过”，让发起人提前看到哪些审批步骤不会真正等人办理。

### 验证说明

- 本轮为后端预览+前端提示改动，尚未打包；前端 dev 热更新后可先看到文案逻辑，后端返回 `-2` 需统一打包后生效。
- 建议用含跳过表达式的流程验证：填不同表单值，右侧预览应随之切换“将自动跳过”。

### 后续目标

1. 继续完善多实例 execution 收敛与字段权限。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：撤回按流程结构选择可达任务

### 本次更新

- 撤回已办任务时不再无条件撤回流程实例内的全部活动任务。
- 后端以“被撤回历史任务”为目标节点，从 BPMN 结构向后查找仍然运行且可达的任务，只撤回这些分支。
- 并行流程中其它分支、以及不能从目标节点结构到达的任务不会被误撤。
- 如果旧模型无法计算可达节点，则回退为原来的“撤回全部活动任务”，保证兼容性。

### 验证说明

- 本轮代码改动尚未打包；按你的要求不再频繁重启，等单个功能完整后统一验证。
- 建议验证：并行分支 A/B，A 已办后继续出现 A 分支下游任务，撤回 A 历史任务时只取消 A 分支下游，B 分支继续运行。

### 已知差距与风险

- 结构可达基于 BPMN 顺序流，不评估条件表达式；需要根据实际条件分支验证。
- 已加签/多实例任务的执行根仍有待专项清理。

### 后续目标

1. 完善多实例 execution 根清理与分支级拒绝。
2. 继续补齐子流程发起人配置、HTTP 回调超时。
3. 推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：后端强制字段写入权限

### 本次更新

- 完成任务、自动通过等写入流程变量前，后端会读取当前 UserTask 的 `formFieldsPermission`。
- 节点没有配置字段权限时行为不变，仍允许全部表单字段写入。
- 节点配置了字段权限时，只有 `WRITE` 字段允许随本次审批写入流程变量；`READ`、`NONE` 以及未出现在权限配置中的字段都会被过滤，避免直接调接口绕过前端只读限制。
- 被过滤字段不会从流程实例变量中删除，只阻止本次请求覆盖它们，因此历史值仍然保留。

### 验证说明

- 本轮后端改动尚未重新编译；下一轮批量编译/重启时一并验证。
- 建议验证：节点配置 `amount=READ`、`reason=WRITE`，通过接口直接提交 `amount=999`，确认流程变量中 `amount` 仍保持原值。

### 已知差距与风险

- 发起流程时的字段权限尚未按首个可写节点限制；当前限制作用于审批完成后的变量写回。
- 字段权限目前仍是 JSON 配置，缺少绑定表单后的可视化选择。

### 后续目标

1. 继续补齐发起流程时的字段权限限制与可视化配置。
2. 完善多实例 execution 收敛、子流程发起人配置。
3. 推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：拒绝/取消流程时显式取消并行与多实例兄弟任务

### 本次更新

- 流程被拒绝或系统跳转到结束节点时，`moveProcessToEnd` 会先遍历活动任务，把“发起拒绝的当前任务之外”的其它并行/会签任务显式标记为“已取消”，原因统一为“流程被拒绝或取消，系统取消任务”。
- 已处于已通过/已拒绝/已取消/已退回/通过中终态的任务不会被覆盖。
- 这样并行分支或多人审批中，一个审批人拒绝后，其它分支任务的详情时间线不再停留在“审批中”。

### 验证说明

- 本轮代码改动尚未重新编译；将在后续批量编译/重启中一并验证。
- 建议构造“并行分支 B、C，B 拒绝”的最小流程，确认 C 的任务在时间线显示“已取消”，流程状态为拒绝。

### 已知差距与风险

- 当前仍是把流程实例内所有其它活动任务都取消；如果后续要支持“分支级拒绝但保留其它分支”，需要新增分支级拒绝语义。

### 后续目标

1. 完善多实例/并行分支的 execution 根清理与分支级拒绝。
2. 继续补齐回调超时、子流程发起人配置。
3. 推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：HTTP 回调触发器等待与续跑接口

### 本次更新

- HTTP 回调触发器现在由两段组成：先执行“发起 HTTP 请求”的 ServiceTask，再进入“HTTP 回调等待”ReceiveTask，流程会停在该等待节点直到外部回调。
- ServiceTask 发出的请求体自动携带 `processInstanceId` 和 `taskDefineKey`；`taskDefineKey` 即等待节点 ID，外部业务拿到后回调本平台即可续跑。
- 新增后端接口 `PUT /bpm/task/trigger-callback?processInstanceId=xxx&taskDefineKey=xxx`，通过 `runtimeService.trigger` 触发 ReceiveTask 并继续后续节点。
- 触发器 JavaDelegate 不再对 HTTP 回调静默放行；配置缺失 URL 时不会继续产生“假通过”。

### 验证说明

- 本轮后端改动尚未重新编译；编译将在下一步完成后统一执行并重启。
- 后续人工验证：配置一个 HTTP 回调触发器指向本地测试接收端，确认收到 `processInstanceId/taskDefineKey`；再调用 trigger-callback 后流程继续到下一节点。

### 已知差距与风险

- 回调接口当前按平台权限控制，外部业务调用时需要携带可访问 token；正式部署如需开放白名单，需要在安全评审后配置。
- 等待节点目前没有独立超时设置；若外部业务一直不回调，流程会永久等待。

### 后续目标

1. 为 HTTP 回调等待节点增加超时边界事件与提醒。
2. 继续完善多实例 execution 收敛、子流程发起人配置。
3. 推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：撤回时显式收敛活动任务状态

### 本次更新

- 撤回已办任务时，会先遍历流程实例当前活动任务，把本次要随活动跳转一起撤掉的任务显式标记为“已取消”，原因统一为“前一节点撤回”。
- 之后再执行 `moveActivityIdsToSingleActivityId` 回到撤回任务节点；即使 Flowable 取消事件没有把每个活动实例都完整落到历史，时间线也能看到被撤回任务不再是“审批中”。
- 显式标记不会覆盖已完成的终态任务；对已经处于已通过/已拒绝/已退回/取消等终态的任务不做重复覆盖。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了静态源码检查与 `git diff --check`。
- 后续人工验证需要重新编译重启后端，发起 A→B→C 流程后在 C 节点撤回 B 的已办，确认 C 的任务在时间线显示为已取消且流程重新回到 B。

### 已知差距与风险

- 当前撤回仍是按“最后完成的任务 + 所有当前活动任务”执行，尚未像芋道那样严格校验串行可达并排除其它并行分支任务；复杂并行场景仍可能把不该撤的任务一起撤回。
- 会签/或签撤回后重新进入节点时的候选人重新计算依赖已加入的运行时重算逻辑，但尚未针对多实例根 execution 做端到端回归验证。

### 后续目标

1. 对齐芋道撤回的“可达性校验 + 并行分支排除”逻辑。
2. 完善多实例根 execution 收敛，并回归退回/撤回/循环场景时间线。
3. 继续推进 HTTP 回调、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：补齐审批超时边界事件与自动处理

### 本次更新

- 简易设计器审批/办理节点新增“审批超时”配置：启用开关、处理方式（自动提醒/自动同意/自动拒绝）、ISO-8601 超时时间、提醒次数。
- 转换器为启用超时的用户任务生成非中断定时边界事件，并把 `boundaryEventType = 1`、`timeoutHandlerType` 写入边界事件扩展元素。
- 自动提醒且次数大于 1 时使用 Flowable 定时周期，达到最大提醒次数后不再重复。
- 新增 `WorkflowTimeoutEventListener`，监听 Flowable `TIMER_FIRED`：
  - 自动提醒：当前记录提醒日志，不自动改变任务；
  - 自动同意：对流程实例中该节点活动任务执行系统自动通过；
  - 自动拒绝：执行系统自动拒绝并结束流程。
- 超时处理调用现有“系统自动完成”入口，不依赖 Shiro 登录用户，因此 Flowable 异步定时任务场景也能执行。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了目标源码对照与静态源码检查。
- 后续人工验证需要重新编译并重启后端，在审批节点启用超时并配置较短的 `PT1M`/`PT2M`，确认超过时间后自动同意或自动拒绝生效；自动提醒时确认日志出现但任务保留。

### 已知差距与风险

- “自动提醒”目前只写日志，尚未接入站内信/短信/邮件提醒；目标版的消息提醒链路仍需后续移植。
- 边界事件基于 Flowable 定时作业，需要异步作业线程已启用；如果部署环境关闭了 Flowable async executor，超时将不会触发。
- 延迟器、子流程超时等其它边界事件类型尚未接入。

### 后续目标

1. 接入超时站内消息/短信提醒，并支持延迟器、子流程超时边界事件。
2. 完善撤回与循环退回的 execution 收敛和时间线。
3. 继续推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：补齐节点执行监听器配置与 BPMN 落库

### 本次更新

- 审批/办理节点新增“执行监听器 JSON”，支持 start、end 等节点执行事件。
- JSON 示例：`[{"eventName":"start","value":"yourExecutionListenerBean"}]`，默认按 Spring Bean 委托表达式写入 Flowable ExecutionListener。
- 也支持 `implementationType: "class"`，直接引用实现 `ExecutionListener`/`JavaDelegate` 的类。
- 执行监听器与上一轮任务监听器会同时写入 UserTask，各自独立触发，不影响全局流程事件监听器。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了静态源码检查与 `git diff --check`。
- 后续人工验证需要注册一个执行监听器 Spring Bean，节点配置 JSON 后重新保存发布，进入/离开节点时观察监听器是否被调用。

### 已知差距与风险

- 监听器参数（FieldExtension）、事务回调、自定义属性解析器尚未支持；完整监听器管理页面仍属于后续配置面板任务。
- 超时/边界事件仍需基于边界定时器实现，下一轮继续处理。

### 后续目标

1. 处理超时与边界事件，并让定时器与节点监听器组合工作。
2. 完善子流程、并行分支、循环退回时间线。
3. 继续推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：补齐节点任务监听器配置与 BPMN 落库

### 本次更新

- 简易设计器审批/办理节点新增“任务监听器 JSON”，可配置任务创建（create）、指派（assignment）、完成（complete）等监听器。
- JSON 支持两种实现方式：
  - `value` 指向已注册 Spring Bean 时，默认按 `delegateExpression` 写入 BPMN；
  - 指定 `implementationType = class` 时，按 Flowable class 监听器写入。
- 转换器会把这些监听器合并到 UserTask 的 Flowable TaskListener 列表中，流程运行时由 Flowable 自动调用，不需要额外引擎旁路。
- 现有自动审批、空审批人处理等全局事件监听器不受影响，JSON 监听器是额外叠加的节点级扩展。

### 验证说明

- 遵照当前要求，本次未执行 Maven/前端编译、单元测试、页面回归、后端重启或数据库操作；仅进行了静态源码检查和 `git diff --check`。
- 后续人工验证需要先注册一个实现 `TaskListener` 的 Spring Bean，例如 `demoTaskListener`，再在节点配置 JSON `[{"eventName":"create","value":"demoTaskListener"}]`，重新保存发布后观察任务创建时是否被调用。

### 已知差距与风险

- 当前只支持任务监听器 JSON 配置；目标版的“流程监听器管理”和可视化监听器事件/值选择面板尚未移植。
- 执行监听器（ExecutionListener）、自定义监听器参数传递还没有接入。

### 后续目标

1. 实现执行监听器与流程级监听器配置，继续向完整监听器管理面板推进。
2. 处理超时/边界事件，完善子流程、并行分支时间线。
3. 继续推进打印模板、流程报表、独立启动器和菜单端到端验收。

## 2026-09-09：任务管理列表处理人姓名显示

### 本次更新

- “流程任务”管理列表的“处理人”列现在优先显示 `assigneeName`/`ownerName`，后端无法解析时才回退到用户编号。
- 与流程详情、加签列表保持一致，管理端不再直接显示一串用户 UUID。

### 验证说明

- 前端 dev 热更新后即可看到该列渲染变化；后端姓名字段需待统一打包后生效。

### 后续目标

1. 继续完善多实例 execution 收敛与字段权限。
2. 推进独立打印模板、流程报表、独立启动器和菜单端到端验收。
## 2026-09-09：简易设计器审批人节点配置面板对齐

### 本次更新

- 简易设计器“审批人”节点改为宽版配置抽屉，按目标界面拆分为“审批人、操作按钮设置、表单字段权限、监听器”四个页签。
- 审批人页签补齐人工审批、自动通过、自动拒绝三种审批类型，并以三列方式展示全部候选人策略；多人依次审批、会签、或签、随机一人等配置继续沿用现有模型协议。
- 拒绝后处理、审批超时、审批人为空、审批人与提交人相同、签名、审批意见、跳过表达式改为目标版的分区布局。
- 操作按钮支持逐项启停和自定义显示名称；显示名称写入 BPMN 扩展配置，并在审批详情页实际显示。
- 表单字段权限不再要求手写 JSON：读取当前流程表单字段后，可逐项设置只读、可编辑、隐藏，也可按列批量设置；旧流程已有权限在未加载表单字段时不会被清空。
- 监听器页签支持创建任务、指派任务执行人员、完成任务三个 POST 回调，包含请求地址、请求头和请求体参数；保存后转换为 Flowable TaskListener 并在对应任务事件执行。
- 保留高级 Flowable 任务监听器/执行监听器 JSON 入口，兼容此前已经保存的监听器配置。
- 自动通过、自动拒绝审批类型已接入任务创建后的自动办理逻辑，不再只是前端选项。

### 验证说明

- 遵照当前要求，本轮未执行 Maven/前端编译、自动化测试、页面回归、服务重启或数据库操作。
- 后续统一验证时应覆盖：四页签保存回显、十五种候选人策略、按钮改名及启停、字段权限、三个任务事件 POST 回调、自动通过和自动拒绝。

### 已知差距与风险

- 任务事件 POST 回调为同步调用；回调地址失败会使对应 Flowable 任务事件失败，正式使用前需要确保接收端稳定。
- 本轮按目标图提供基础监听器参数配置，尚未加入重试次数、超时、签名鉴权等增强项。

## 2026-09-10：自动审批节点隐藏人工审批配置

### 本次更新

- 简易设计器审批节点选择“自动通过”或“自动拒绝”时，不再显示“审批人、操作按钮设置、表单字段权限、监听器”等仅适用于人工审批的配置页签。
- 切回“人工审批”时恢复原有配置页签；切换审批类型不会主动清空已填写的审批人、按钮、字段权限和监听器配置，避免误操作造成配置丢失。
- 条件显示逻辑与芋道目标前端保持一致，未改动审批节点的保存协议和运行时自动办理逻辑。

### 验证说明

- 已完成目标源码与当前组件的静态对照；尚未执行前端编译和浏览器页面回归。
- 后续人工验证：打开简易设计器审批节点，依次选择“人工审批、自动通过、自动拒绝”，确认仅人工审批显示四个配置页签，切回人工审批后原配置仍可见。

### 后续目标

1. 完成审批人配置面板统一编译与页面回归。
2. 继续补齐流程报表、打印模板运行闭环和独立工作流启动器。

## 2026-09-10：补齐流程模型数据报表入口与动态字段查询

### 本次更新

- 已发布流程模型的“更多”菜单新增“报表”入口；仅拥有流程实例管理员查询权限的用户可见，跳转参数与芋道目标版一致，携带流程定义编号和流程标识。
- 新增隐藏路由“数据报表”，页面按指定流程标识查询该流程全部历史版本的实例，不在左侧菜单重复增加独立菜单项。
- 报表提供发起人、流程名称、流程状态、发起时间、结束时间筛选，并解析流程表单配置，为文本字段动态增加查询条件。
- 报表表格根据流程表单动态增加字段列，数据直接读取实例 `formVariables`；保留流程详情与运行中流程的管理员取消操作。
- 复用后端现有 `manager-page` 查询能力。该接口已经实现 `formFieldsParams` 安全解析、字段数量限制、系统变量过滤及 Flowable 历史变量精确筛选，本轮未新增重复后端查询逻辑。
- 修正流程实例列表返回值：将 Flowable 已加载的普通业务变量写入 `formVariables`，并剔除 `_workflow` 前缀和状态、意见等系统保留变量，使报表动态列能够显示实际表单数据，同时避免泄露内部控制变量。
- 新增 Flyway 增量迁移，为已有流程实例管理员角色分配隐藏报表路由；未修改已经发布的历史迁移脚本，也未扩大管理员查询权限本身。

### 验证说明

- 本轮未执行前端编译、Maven 编译、页面回归、服务重启或数据库迁移；按当前约定留待功能批次完成后统一验证。
- 后续人工验证：发布一个包含文本字段的流程，从流程模型“更多 → 报表”进入，按发起人/状态/时间/表单字段查询，并检查动态列、详情和取消操作。

### 后续目标

1. 完善打印模板在流程详情中的自定义模板渲染和打印闭环。
2. 实现独立 Spring Boot 工作流服务启动器并明确其系统主数据依赖边界。
3. 统一编译后继续按菜单逐页进行端到端验收。

## 2026-09-10：打通打印模板运行闭环

### 本次更新

- 后端打印数据接口 `/bpm/process-instance/get-print-data` 不再固定返回“未开启打印模板”，改为按流程定义反查流程模型的 `printTemplateSetting`：`enable` 为真时返回 `printTemplateEnable=true` 及模板 HTML，与目标版“流程模型 → 自定义打印模板 → 实例打印”的数据链路一致。
- 打印数据新增发起人所属部门（`startUserDeptName`）：仅在打印接口内解析，复用系统部门接口按用户名查询，避免在实例列表查询中引入逐行部门查询开销。
- 流程详情页“打印”按钮由直接调用浏览器打印改为打开打印弹窗，弹窗数据全部来自后端打印接口，不再依赖页面当前渲染状态。
- 打印弹窗默认模板按目标版信息结构输出：流程标题、流程编号、打印人员、发起人、发起时间、所属部门、流程状态、表单内容、流程记录；流程记录逐行展示处理人/节点/处理时间/任务状态/处理意见，并在存在手写签名时打印签名图片。
- 打印弹窗支持自定义打印模板：模板开启时优先渲染模板 HTML，并支持 `${processName}`、`${processNum}`、`${startUser}`、`${startUserDept}`、`${startTime}`、`${endTime}`、`${processStatus}`、`${printUser}`、`${printTime}` 占位符替换；未命中内置占位符时按流程表单变量取值。
- 自定义模板渲染做了安全收敛：渲染前移除 `script`/`iframe`/`object`/`embed`/`link`/`style` 标签与内联 `on*` 事件属性、拦截 `javascript:` 链接，占位符取值统一做 HTML 转义，避免模板配置者在他人会话中注入脚本。
- 表单字段标题解析复用流程定义的表单配置，字段值按数组、布尔、对象分别格式化；未配置设计器表单时退回按变量名展示，与流程详情页的降级行为保持一致。
- 打印动作复用项目既有 `printNb` 打印能力（`vue-print-nb-jeecg` 已在依赖中），打印容器独立成 `#bpmPrintContainer`，避免把审批操作区、评论框等无关内容带入打印结果。

### 验证说明

- 本轮未执行前端编译、Maven 编译、页面回归、服务重启或数据库迁移；按当前约定留待功能批次完成后统一验证。
- 后续人工验证：为一个已发布流程开启自定义打印模板并填写模板内容，发起并完成若干审批节点后，在流程详情点击“打印”，分别确认“默认模板”和“自定义模板”两条路径的字段值、流程记录、签名图片与最终打印预览。

### 后续目标

1. 实现独立 Spring Boot 工作流服务启动器并明确其系统主数据依赖边界。
2. 统一编译后继续按菜单逐页进行端到端验收。
3. 继续补齐会签/或签多实例、候选人策略、退回撤回跳过与边界事件等运行时能力。

## 2026-09-10：办理人节点面板与画布删除操作对齐

### 本次更新

- 简易设计器的“办理人”节点改用宽版配置抽屉，与“审批人”共用候选人策略、多人办理方式、办理人为空时、表单字段权限和监听器配置界面。
- 办理人模式隐藏审批类型、审批按钮、审批签名、审批意见、审批人超时和审批人专属退回配置，保留截图中的办理人配置项；切换到审批人节点时原审批配置不受影响。
- 节点卡片右上角增加可访问的“×”删除按钮；删除串行节点后自动接续其下一个节点，分支内节点也按同样规则处理。
- 移除抽屉底部“删除节点”按钮，节点删除统一从画布完成，避免配置抽屉与画布操作入口不一致。

### 验证说明

- `eslint` 已通过：`SimpleProcessTree.vue`、`SimpleProcessDesigner.vue`。
- `ApprovalNodeConfig.vue` 保留了项目原有的 `vue/no-mutating-props` 历史告警，本轮未扩大该问题范围；未执行全量前端构建和浏览器回归。
- 后续人工验证：打开简易设计器新增/编辑办理人节点，确认三列办理人策略、多人办理方式和“办理人为空时”布局；在画布点击节点右上角“×”，确认节点删除及后续节点接续。

### 后续目标

1. 统一完成前端编译和页面回归后再重启服务。
2. 继续补齐候选人策略及会签/或签的运行时闭环。

### 补充修正

- 指定成员、指定角色、指定岗位以及“办理人为空时 → 指定成员审批”原本已有数据保存和运行时解析，但选择器调用使用了布尔值 `true`；已统一改为底层 `JSelectBiz` 识别的 `multiple="multiple"`，确保多选弹窗、标签回显和逗号分隔保存一致。
- 画布删除按钮改为 CSS 绘制的对称交叉线，不再依赖字体“×”的字形和基线。

### 补充验证

- `SimpleProcessTree.vue`、`SimpleProcessDesigner.vue` ESLint 通过。
- `git diff --check` 通过；尚未进行浏览器交互回归。

## 2026-09-10：修复指定角色标签删除

### 本次更新

- 修复 `JSelectRole` 未转发 `JSelectBiz` 的 `change` 事件问题：点击角色标签右侧 X 后，现在会同步更新父级流程节点的 `candidateParam`，不会被旧值重新回显。
- 保留角色弹窗确认时的数组回传，并兼容流程节点使用逗号分隔字符串保存的格式。

### 验证说明

- `JSelectRole.vue` ESLint 无错误，仅保留组件原有事件命名提示。
- `git diff --check` 通过；未执行浏览器交互回归。

## 2026-09-10：补充分支节点删除入口

### 本次更新

- 条件分支、并行分支、包容分支、路由分支节点现在也显示画布右上角删除键。
- 删除分支节点后会自动接续其后续节点；分支节点内部的条件项仍通过“分支管理”删除，避免误删单个条件项时破坏分支结构。

### 验证说明

- 已通过修改范围内的 ESLint 和 `git diff --check` 检查；未执行浏览器交互回归。

## 2026-09-10：保护条件分支“其他情况”兜底分支

### 本次更新

- 条件、并行、包容、路由分支的最后一个兜底分支（通常显示为“其他情况”）现在不可删除，与芋道目标版保持一致。
- 普通条件分支仍可删除；当流程只剩两个分支时也继续禁止删除，避免分支结构不足。
- 删除逻辑增加二次保护，即使绕过按钮状态调用，也不会误删兜底分支。

### 验证说明

- 已通过 `SimpleProcessDesigner.vue` ESLint 和 `git diff --check` 检查；未执行浏览器交互回归。

## 2026-09-10：补充自定义表单路径说明

### 本次更新

- 在流程模型向导的“业务表单”配置中，为“提交页面路径”增加 Vue 路由地址说明及 `/bpm/oa/leave/create.vue` 示例。
- 为“查看页面路径”增加 Vue 组件地址说明及 `/bpm/oa/leave/detail.vue` 示例。
- 仅增加界面提示，不改变路径字段的保存、校验和运行时跳转逻辑。

### 验证说明

- 已检查提示文本和现有表单绑定；未执行浏览器交互回归。

## 2026-09-10：接入课题审批业务表单

### 本次更新

- 复用 `weather-planfrom-frontend` 的课题在线申报界面结构，在 Jeecg 新增独立业务表单页面：`/bpm/topic-declaration/create.vue`。
- 新页面覆盖课题基本信息、研究目标与内容、科研团队、成果指标、预算和阶段里程碑等主要填报区域。
- 页面通过现有 `/bpm/process-instance/create` 统一发起入口传入 `processDefinitionId` / `processDefinitionKey`，提交时调用现有 `startProcess` 接口。
- 提交数据同时以流程变量和 `topicApplication` 对象传入，流程实例名称使用课题名称；业务标识继续遵循当前后端规则，由后端生成流程实例 UUID。
- 增加审批路径预览和“发起人选择审批人”支持；流程详情页识别 `/bpm/topic-declaration/detail.vue` 时使用同一表单只读展示，并保留右侧审批操作。

### 使用方式

- 在流程模型“业务表单”中选择自定义表单类型。
- 提交页面路径填写：`/bpm/topic-declaration/create.vue`。
- 查看页面路径填写：`/bpm/topic-declaration/detail.vue`。
- 从“审批中心 → 发起流程”进入该流程，填写课题表单并提交。

### 验证说明

- 新增页面已通过修改范围内的 ESLint 检查；未执行全量前端构建和浏览器回归。
- 当前版本把表单数据保存为流程变量，尚未新增独立课题业务表及业务数据 CRUD；如需列表查询、草稿、归档和业务详情持久化，需要继续补充后端业务表。

## 2026-09-11：完成简易设计器发起人节点配置面板

### 本次更新

- 新增 `StartUserNodeConfig.vue`，发起人节点点击后显示可发起范围摘要、表单字段权限矩阵和固定“提交”按钮说明。
- 表单字段权限支持逐字段设置“只读 / 可编辑 / 隐藏”，并支持按列批量设置；默认新字段为可编辑，保存结构与后端 `fieldsPermission` 约定一致。
- 简易设计器接收模型向导和独立设计入口的 `startUserIds`、`startDeptIds`，发起权限仍由基本信息步骤统一维护，避免在画布抽屉中产生两套权限来源。
- 为发起人节点补充 `fieldsPermission`、`buttonsSetting` 默认值，旧流程 JSON 打开时自动补齐，不影响已有流程节点。

### 验证说明

- 已通过 ESLint：`StartUserNodeConfig.vue`、`SimpleProcessDesigner.vue`、`ModelWizard.vue`、`model/index.vue`。
- 已通过 `git diff --check`；未执行全量前端构建和浏览器回归，避免在单功能未完成前反复打包。

### 后续目标

1. 进入 BPMN 设计器：补齐用户任务的候选人策略、审批方式、字段权限、超时/退回和监听器属性面板，并保持 XML 可部署。
2. 在 BPMN 设计器完成最小可用配置后，再继续运行时语义和端到端验收。

## 2026-09-11：启动 BPMN 设计器属性面板对齐

### 本次更新

- BPMN 用户任务右侧属性面板新增候选人策略、候选参数、多人审批方式、会签通过比例、审批人为空处理、跳过表达式、字段权限 JSON 和超时处理配置。
- 候选策略、审批方式及字段权限通过 Flowable 扩展元素写入 BPMN XML；跳过表达式使用 `flowable:skipExpression` 标准任务属性，重新打开模型时可以回显。
- 保留 BPMN 原生画布、撤销/重做、缩放和条件流转配置；不改变模型向导对 BPMN XML 的保存入口。

### 当前边界

- 本轮是 BPMN 设计器的第一阶段：已打通用户任务核心属性编辑和 XML 回写，尚未完成 BPMN 专用的监听器、边界事件图形化配置、子流程/并行分支配置面板。
- 超时处理字段当前先保存为 Flowable 扩展元数据；边界事件的可视化创建和连线将在下一阶段实现，避免把未完成的运行语义伪装成已完成。

### 验证说明

- 已通过 ESLint：`BpmnDesigner.vue`、`flowableDescriptor.ts`。
- 已通过 `git diff --check`；未执行全量前端构建和浏览器回归。

## 2026-09-11：BPMN 超时边界事件基础闭环

### 本次更新

- BPMN 用户任务的“启用超时”现在会在画布上创建真实的 `bpmn:BoundaryEvent` + `bpmn:TimerEventDefinition`，关闭时删除对应事件。
- 超时时间写入定时器的 `timeDuration`，自动提醒/自动同意/自动拒绝写入边界事件的 `timeoutHandlerType`，与后端 `WorkflowTimeoutEventListener` 使用的扩展约定一致。
- 重新选中用户任务时会从已存在的边界事件回显启用状态和时间表达式；边界事件保留原生 BPMN 连线能力，用户可从事件拖出后续处理路径。
- `flowableDescriptor.ts` 增加 `flowable:skipExpression` 属性定义，跳过表达式可由 BPMN 设计器标准属性回写。

### 当前边界

- 当前完成的是单个用户任务的定时边界事件创建、删除和属性回显；尚未提供边界事件专用的动作向导、事件连线自动生成和非定时边界事件类型（错误、消息、信号）。

### 验证说明

- 已通过修改范围内的 ESLint（前一阶段及本阶段静态检查）和 `git diff --check`；未执行全量构建、浏览器回归或服务重启。

## 2026-09-11：修正本地前端启动目录

### 本次更新

- 发现前端启动命令未切换到 `jeecgboot-vue3` 目录时，Vite 会在错误目录运行并使根路径返回 404。
- 已停止错误实例并在前端项目目录重新启动 Vite；后端保持原实例运行，未重新打包或修改业务代码。

### 验证说明

- `http://localhost:3111/` 返回 HTTP 200。
- `http://localhost:8880/jeecg-boot/` 返回 HTTP 200。

### 后续目标

1. 继续补齐 BPMN 设计器的监听器、子流程/并行配置和运行时语义。
2. 完成统一编译后再按菜单逐页进行端到端验收。

## 2026-09-11：补强会签/或签取消实例状态

### 本次更新

- Flowable 的并行或签/会签在满足完成条件后会取消剩余任务，但取消事件的 executionId 可能指向多实例根执行，单靠历史活动 executionId 无法找到所有子任务。
- `WorkflowTaskAssignmentEventListener` 现在在处理活动取消事件时，除历史活动查询外，会按流程实例 ID 与活动节点 ID补查仍处于活动状态的任务，并统一写入“已取消”状态和系统取消原因。
- 该兜底保持幂等：已经是审批、拒绝、退回或取消状态的任务不会被覆盖。

### 验证说明

- 按当前约定未执行 Maven 单元测试、全量编译或服务重启；本次修改包含针对该事件路径的单元测试用例，待功能批次完成后统一执行。

### 后续目标

1. 继续完善会签/或签的拒绝、比例完成和退回重建实例语义。
2. 补齐 BPMN 设计器监听器、子流程/并行配置面板及运行时闭环。

## 2026-09-11：扩展 BPMN 边界事件配置面板

### 本次更新

- BPMN 设计器选中边界事件时新增事件类型、事件标识/条件、定时器时长和“触发后取消附着节点”配置。
- 支持定时器、错误、消息、信号和条件事件定义的创建与切换，配置直接写回 BPMN `eventDefinitions`，保持 Flowable 原生事件语义。
- 切换为非定时事件时会清理超时专用扩展属性，切回定时器时重新写入默认超时处理类型，避免旧配置残留。
- 保留画布连线方式：事件触发后的处理路径仍由设计人员从边界事件拖出连线，避免自动猜测业务分支。

### 验证说明

- 按当前约定未执行前端构建、Maven 测试或浏览器回归；本轮修改范围限于 BPMN 属性面板与进度文档。

### 后续目标

1. 继续补齐 BPMN 子流程、并行网关和监听器的结构化配置面板。
2. 将非定时边界事件加入流程实例时间线和异常处理验证，再统一编译验收。

## 2026-09-11：增加消息/信号边界事件触发接口

### 本次更新

- 新增 `PUT /bpm/task/trigger-event`，按流程实例、事件类型（`message`/`signal`）和事件名称查找 Flowable 事件订阅并触发，支持同时传入普通流程变量。
- 事件变量沿用流程变量安全校验，禁止覆盖 `PROCESS_`、`TASK_` 和 `_workflow` 系统变量；没有匹配订阅或流程已结束时返回明确业务错误。
- 该接口与 BPMN 设计器新增的消息/信号边界事件定义配套，外部业务系统可在回调/消息到达后让流程继续执行。

### 验证说明

- 按当前约定未执行 Maven 测试、全量编译或接口回归；接口契约已放入 `workflow-api`，控制器仅声明 `bpm:task:update` 权限。

### 后续目标

1. 将消息/信号事件触发结果补充到流程详情时间线，并完善外部回调的独立鉴权方式。
2. 继续补齐 BPMN 子流程、并行网关和结构化监听器配置。

## 2026-09-11：修复前端依赖后恢复 3111 访问

### 本次更新

- 定位到 Vite 启动失败原因为自定义主题插件直接引用 `debug`、`chalk`、`clean-css`，但前端 `package.json` 未声明这三个构建依赖，导致 `vite.config.ts` 加载时报 `MODULE_NOT_FOUND`。
- 在前端开发依赖中补充锁定兼容版本，并使用项目锁文件重新生成 `node_modules` 链接；未升级其它业务依赖。
- 前端 Vite 已绑定 `0.0.0.0:3111`，后端保持现有 `8880` 实例不变。

### 验证说明

- `http://localhost:3111/`、`http://127.0.0.1:3111/` 和局域网地址 `http://192.168.1.58:3111/` 均返回 HTTP 200。
- 按当前约定未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 保持前后端开发实例运行，待单项功能批次完成后再统一编译重启。
2. 继续推进 BPMN 子流程、并行网关和监听器配置闭环。

## 2026-09-11：补齐 BPMN 调用活动与网关配置入口

### 本次更新

- BPMN 设计器选中 `CallActivity` 时新增被调用子流程、流程实例名称、继承变量、继承业务标识、业务标识表达式以及输入/输出参数配置。
- 输入/输出参数以 `flowable:In`、`flowable:Out` 扩展元素写回，使用 `source`/`target` 字段，兼容 Flowable 调用活动的变量映射语义。
- 子流程下拉列表复用流程模型接口；列表不可用时仍可保留已有 XML 配置，不影响其它节点编辑。
- 选中排他/包容/并行网关时新增默认流转分支选择，并根据网关现有出线显示分支名称和连线 ID；连线条件仍在连线属性中维护。
- Flowable moddle 描述补充调用活动扩展属性，确保继承变量、继承业务标识和业务键可序列化。

### 验证说明

- Vite 开发服务器已通过 HMR 重新加载 `BpmnDesigner.vue`，当前 `http://localhost:3111/` 返回 HTTP 200。
- 按当前约定未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 将调用活动的子流程实例状态、变量映射和异常结果补充到流程详情时间线。
2. 继续补齐脚本/接收/发送任务及监听器的结构化配置，并统一处理 Flowable 多实例完成条件。

## 2026-09-11：补齐 BPMN 脚本任务配置

### 本次更新

- BPMN 设计器选中 `ScriptTask` 时新增脚本格式、内联/外部资源模式、脚本内容/资源地址和结果变量配置。
- 切换脚本模式时会清理另一种模式的 BPMN 属性，避免同时保存 `script` 与 `resource` 导致 Flowable 解析歧义。
- 配置直接写回标准 BPMN 脚本任务属性，保留现有服务任务、用户任务和事件面板行为。

### 验证说明

- Vite 开发服务器已通过 HMR 重新加载设计器文件；未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 增加接收任务的消息/信号引用管理，并与消息/信号事件触发接口打通。
2. 将监听器、服务任务 HTTP 参数和网关条件配置继续结构化，减少手写 JSON。

## 2026-09-11：接收任务消息引用配置

### 本次更新

- BPMN 设计器选中 `ReceiveTask` 时新增消息名称配置；保存时会在定义根元素中创建或复用 `bpmn:Message`，并将 `messageRef` 绑定到接收任务。
- 清空消息名称会移除接收任务的消息引用，避免流程发布后留下不可触发的旧订阅。
- 面板提示与 `/bpm/task/trigger-event` 的 `eventType=message`、`eventName` 参数保持一致，外部系统可以按设计器中配置的名称触发流程。

### 验证说明

- Vite 开发服务器已通过 HMR 重新加载设计器文件；未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 增加中间捕获事件的消息/信号引用和启动事件配置，覆盖非 ReceiveTask 的事件等待场景。
2. 将服务任务 HTTP 配置和任务监听器从 JSON 文本进一步结构化，并补充运行时异常记录。

## 2026-09-11：完善 BPMN 服务任务执行配置

### 本次更新

- BPMN 设计器的服务任务面板支持 Java 类、普通表达式、代理表达式和 HTTP 调用四种执行类型。
- HTTP 类型支持请求方法、请求地址、请求头 JSON、返回变量前缀和忽略异常选项，并写入 Flowable `flowable:Field` 扩展；切换到其它类型时会清理 HTTP 专用字段。
- 重新选中已有服务任务时会从标准属性和扩展字段回显执行类型及 HTTP 配置。
- Flowable moddle 描述补充服务任务的 `flowable:type` 属性，保留其它扩展元素不被覆盖。

### 验证说明

- Vite 开发服务器已连续通过 HMR 重新加载设计器文件，当前 3111 端口保持监听；未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 将中间捕获事件、发送任务和消息/信号引用统一接入事件定义面板。
2. 补齐独立工作流启动器的身份、租户、通知和文件适配器边界，避免只能依赖系统业务模块运行。

## 2026-09-11：补齐 BPMN 中间与边界外事件定义面板

### 本次更新

- BPMN 设计器为中间捕获事件、中间抛出事件、启动事件和结束事件增加消息、信号、定时器、条件四类事件定义配置。
- 消息/信号配置会自动创建或复用 `bpmn:Message` / `bpmn:Signal` 根元素并绑定 `messageRef` / `signalRef`，事件名称可直接作为外部触发名使用。
- 定时器和条件表达式写入标准 BPMN 事件定义；清空类型会移除原事件定义。
- 发送任务复用服务任务执行面板，避免设计器只能配置 `ServiceTask` 而无法配置 `SendTask`。

### 验证说明

- Vite 开发服务器已通过 HMR 重新加载事件面板，当前 `http://localhost:3111/` 返回 HTTP 200；未执行前端构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 将事件触发结果、子流程状态和并行分支状态写入统一流程时间线。
2. 继续完成独立工作流启动器及外部身份/租户适配器，并在功能批次结束后统一编译验证。

## 2026-09-11：补齐消息启动事件前端调用契约

### 本次更新

- 前端 `workflow.api.ts` 新增 `WorkflowStartEventRequest` 类型，统一消息名称、业务标识、实例名称和流程变量的请求结构。
- 新增 `startProcessByMessage` API，调用后端 `/bpm/process-instance/start-by-message`，与 BPMN 设计器中配置的消息启动事件及接收任务消息名称保持一致。
- 该 API 只补齐调用契约，不改变现有普通流程发起入口；业务页面可以按需传入消息名称和变量，避免重复拼装请求。

### 验证说明

- 已确认 Vite 开发服务器在 `3111` 端口监听，前端根路径和 BPMN 路径返回 HTTP 200；未执行前端构建、Maven 测试或浏览器端到端回归，遵循当前“功能批次完成后统一验证”的约定。

### 后续目标

1. 将消息启动 API 接入流程发起页面的事件型流程入口，并在流程详情时间线显示由消息启动的实例。
2. 继续补齐并行/多实例运行状态、抄送与触发器运行语义，随后统一进行一次编译验证。

## 2026-09-11：补齐事件触发前端 API 契约

### 本次更新

- 前端工作流 API 新增 `WorkflowEventTriggerRequest` 类型，明确流程实例、`message`/`signal` 事件类型、事件名称和变量结构。
- 新增 `triggerEvent` 与 `triggerCallback` 方法，分别对应消息/信号边界事件触发和 HTTP 回调等待节点唤醒接口。
- 修正 BPMN 接收任务清空消息名称时的引用清理，使用 `null` 清除 `messageRef`，避免旧消息引用继续被序列化。
- 流程定义返回值新增 `messageStartNames`，发起流程页据此显示“用户发起 / 消息启动”切换，并把发起人自选审批人一并传给消息启动接口。
- 该改动把已有后端运行时能力暴露给业务页面和外部适配页面，未改变现有任务办理流程和权限校验。

### 验证说明

- Vite 开发服务器仍在 `3111` 端口运行并返回 HTTP 200；按约定未执行全量构建、Maven 测试或浏览器端到端回归。

### 后续目标

1. 在流程详情时间线补充抄送、触发器、调用活动和边界事件的运行记录展示。
2. 完成独立工作流服务启动器的适配器边界设计，再统一编译验证。

## 2026-09-11：新增独立工作流 Spring Boot 启动器骨架

### 本次更新

- 在 `jeecg-module-workflow/workflow-starter` 新增独立启动模块和 `WorkflowStandaloneApplication`，默认端口为 `8890`，不复用单体 `jeecg-system-start` 启动类。
- 启动器排除 Jeecg 本地 system 身份适配器，提供可替换的 `WorkflowIdentityAdapter` / `WorkflowCandidateIdentityAdapter` 默认实现，并保留租户、用户、角色、部门配置入口。
- 对 workflow-biz 仍保留的 `ISysBaseAPI` 兼容点提供开发用空代理，避免启动器在没有 `system-biz` 时因 Bean 缺失直接失败；生产环境必须替换为真实用户中心适配器。
- 增加独立启动器 README 和示例 `application.yml`，明确数据库配置、适配器替换要求及与 8880 单体端口的隔离方式。

### 验证说明

- 未执行 Maven 编译或启动独立服务，遵循当前“功能批次完成后统一打包”的约定；当前 8880 后端和 3111 前端运行实例未受影响。

### 后续目标

1. 在统一编译批次中验证启动器的 Bean 扫描、Flowable 数据源和外部身份适配器替换。
2. 补充调用活动、抄送、触发器和边界事件的流程详情时间线记录。

## 2026-09-11：处理挂起任务清理异常

### 本次更新

- 修复清理流程模型实例时对已挂起任务写入取消状态导致的 `Cannot add variables to a suspended Task` 异常。
- `ACTIVITY_CANCELLED` 监听器在补写任务展示变量前检查 `Task.isSuspended()`；任务已经挂起或并发切换为挂起时跳过变量写入，让 Flowable 正常完成实例删除。
- 申请人取消流程的任务变量写入同样跳过挂起任务，避免取消入口和模型清理入口出现不一致的异常。
- 对 Flowable 抛出的挂起任务变量异常做幂等处理；仅忽略该类任务状态竞争，其他 Flowable 异常继续抛出，避免掩盖真实错误。

### 验证说明

- 已完成静态路径检查，确认模型清理会经过运行实例删除和活动取消监听器；未重启后端，未执行 Maven 构建、测试或数据库清理，遵循当前“功能批次完成后统一打包”的约定。

### 后续目标

1. 在下一次统一编译/重启批次验证挂起流程、普通运行流程和历史实例清理三种路径。
2. 继续完成候选人策略及多实例运行语义闭环。

## 2026-09-11：将抄送记录接入流程详情时间线

### 本次更新

- 审批详情返回值新增 `copies`，按当前租户和流程实例查询抄送记录，并按产生时间排序。
- 办理时抄送和简易设计器抄送节点写入的记录都复用同一查询入口，详情页新增“抄送记录”区块，显示节点、接收人、时间和原因。
- 详情接口先执行原有流程实例访问权限校验，再读取抄送记录，未扩大流程数据访问范围。

### 验证说明

- 已完成接口调用链和前端类型/模板静态检查；未重启后端，未执行 Maven 构建、前端构建或端到端测试，遵循当前功能批次统一验证约定。

### 后续目标

1. 将触发器、边界事件和子流程运行记录以同样的时间线事件模型补齐。
2. 继续完善 Flowable 多实例完成条件及并行/循环退回的历史状态展示。

## 2026-09-11：补充非审批节点运行时间线

### 本次更新

- 新增 `WorkflowTimelineEventVO`，从 Flowable 历史活动读取网关、子流程、调用活动、服务/发送/接收任务、边界事件及开始/结束事件。
- 审批详情新增 `timelineEvents`，与原有审批节点时间线分开，避免把非用户任务误显示为审批人任务。
- 流程详情页新增“运行事件”区块，显示事件名称、类型、开始/结束时间和当前是否仍在运行，为并行分支、子流程和边界事件提供可追踪记录。

### 验证说明

- 已完成历史查询、DTO、接口返回和前端展示链路静态检查；未重启后端，未执行 Maven 构建、前端构建或端到端测试，遵循当前功能批次统一验证约定。

### 后续目标

1. 将事件定义中的触发结果、回调变量和边界事件处理结果写入同一事件详情。
2. 继续补齐会签/或签完成条件的运行计数和循环退回的历史关联。

## 2026-09-11：稳定会签比例完成条件

### 本次更新

- 会签（按比例通过）的 Flowable `completionCondition` 从浮点除法改为整数比较：`nrOfCompletedInstances * 100 >= nrOfInstances * approveRatio`。
- 保留或签“首个子任务完成即结束”和顺序审批“全部实例完成”的语义，避免仅因 EL 引擎整数除法差异导致会签节点迟迟不收敛。
- 该修正只影响后续发布的流程定义，不改写数据库中已经部署的 BPMN 版本。

### 验证说明

- 已静态核对简易设计器 `approveMethod/approveRatio` 到 Flowable 多实例完成条件的生成链路；未重启后端，未执行 Maven 构建、测试或重新部署流程，遵循当前功能批次统一验证约定。

### 后续目标

1. 在统一运行批次中验证会签比例、或签首个通过和顺序审批三种实例收敛路径。
2. 补充多实例当前/已完成/已取消计数到流程详情和运行事件中。

## 2026-09-11：独立启动器补齐默认身份代理

### 本次更新

- 独立 `workflow-starter` 不再只提供全空的 `ISysBaseAPI` 代理；默认配置现在可以返回用户、角色、部门、岗位、部门负责人、父部门和部门名称映射。
- `jeecg.workflow.standalone` 增加 `user-names`、`role-users`、`department-users`、`position-users`、`department-leaders`、`parent-departments` 和 `department-names` 配置，候选人策略和连续多级部门负责人可以在不引入 `system-biz` 的情况下完成最小验证。
- 评论、抄送和流程详情中的用户校验/名称解析复用同一代理；真实部署仍可注入远程用户中心适配器替换默认实现。

### 验证说明

- 已完成启动器 Bean、属性绑定和 `ISysBaseAPI` 调用面的静态核对；未执行 Maven 编译、数据库迁移或服务重启，遵循当前“功能批次完成后统一打包”的约定。

### 后续目标

1. 在统一打包批次验证独立启动器的 Flowable 数据源和候选人策略运行。
2. 继续补齐多实例运行计数、退回/撤回以及设计器配置闭环。

## 2026-09-11：在流程详情展示多实例运行计数

### 本次更新

- 流程详情的审批节点现在标记是否为 Flowable 多实例节点，并返回总实例数、已完成实例数、活动实例数和完成百分比。
- 运行中的会签/或签优先读取 Flowable 的 `nrOfInstances`、`nrOfCompletedInstances`、`nrOfActiveInstances`；历史节点没有运行时变量时，回退使用历史任务实例统计，兼容已结束流程。
- 前端流程图节点显示“已完成/总数”计数，不改变现有办理按钮和页面结构，便于确认会签比例、或签首个完成和顺序审批的实际收敛状态。

### 验证说明

- 已完成 DTO、服务查询、前端类型和节点展示的静态链路检查；未执行 Maven 编译、前端构建或端到端测试，仍按功能批次统一验证。

### 后续目标

1. 将多实例完成条件和取消实例的原因继续写入审批时间线。
2. 继续补齐超时/边界事件和退回后多实例重新创建的运行语义。

## 2026-09-11：补齐延迟器与子流程边界超时语义

### 本次更新

- 增加边界事件类型常量：延迟器超时（2）和子流程超时（3），与目标版 Flowable 约定一致。
- 简易设计器的子流程 `timeoutSetting` 现在会生成固定时长或固定日期的非中断边界定时器；配置缺失或类型非法时在发布前明确报错。
- 超时监听器新增延迟等待节点触发和子流程结束处理：延迟边界事件触发对应 execution，子流程边界超时只结束当前 CallActivity 对应的子流程，不影响同一主流程的其他子流程。
- 审批超时提醒除日志外写入流程评论 `timeout-reminder`，使提醒行为可在流程详情中追溯；自动通过/拒绝仍复用既有审批事务。

### 验证说明

- 已完成目标版 `BpmTaskEventListener`、`SimpleModelUtils` 与本项目转换器/监听器的静态对照，并通过 `git diff --check`；未执行 Maven 编译、数据库迁移或运行时定时任务测试，遵循功能批次统一验证约定。

### 后续目标

1. 将边界事件触发结果补入流程运行事件时间线，并处理重复触发的幂等记录。
2. 继续补齐退回/撤回后的多实例重建和子流程历史关联。

## 2026-09-11：后端落实节点操作按钮权限

### 本次更新

- 审批、拒绝、退回、转办、委派、加签和抄送接口现在会读取流程节点的 `enabledButtons` 扩展配置，在服务端再次校验；前端隐藏按钮不再是唯一限制。
- 未配置按钮列表的旧流程保持原有默认行为；配置了列表但未包含当前操作时返回明确业务错误。
- 自动通过/自动拒绝不受人工按钮限制，继续作为节点配置驱动的内部运行操作。

### 验证说明

- 已完成控制器→服务→BPMN 扩展属性的静态调用链检查，并通过 `git diff --check`；未执行 Maven 编译、前端构建或端到端测试。

### 后续目标

1. 将字段权限从“过滤提交变量”扩展到表单读取和必填校验，并补齐自定义表单的权限契约。
2. 将节点监听器执行结果、按钮操作和边界事件统一写入运行时间线。

## 2026-09-11：流程监听器模板接入 BPMN 运行定义

### 本次更新

- BPMN 设计器选中流程根节点时，可直接选择已启用的“执行监听器模板”；保存时会生成 Flowable `ExecutionListener`，发布后由 Flowable 按开始、结束或流转事件执行。
- 监听器模板服务新增保存前校验：Java 类必须实现对应的 `JavaDelegate`/`TaskListener`，表达式必须使用 `${...}` 格式，避免错误配置拖到发布或启动阶段才失败。
- 具体用户任务仍可使用节点级任务/执行监听器配置；流程监听器模板只筛选执行类型，避免把任务监听器错误挂到流程根节点。

### 验证说明

- 已完成流程监听器管理页、BPMN 设计器、Flowable 扩展 XML 和服务端校验的静态调用链检查，并通过 `git diff --check`；未执行 Maven 编译、前端构建或端到端测试，遵循功能批次统一验证约定。

## 2026-09-11：候选人策略与自选审批表达式稳定性

### 本次更新

- 对照目标版候选人策略编号，确认指定用户、角色、部门成员、部门负责人、连续多级部门负责人、岗位、用户组、发起人/审批人自选、发起人本人、表单字段、流程表达式等均通过 `WorkflowCandidateIdentityAdapter` 解析，宿主用户与组织数据仍可替换适配器提供。
- 连续部门负责人按 `[当前部门, 父部门, ...]` 去重收集；指定层级策略只取目标层级，找不到更高父级时回退到已到达的最高层级，和目标版行为一致。
- 发起人自选、审批人自选的单实例节点改用统一候选人解析表达式；未提交选择时不再因直接访问缺失的 Map 下标导致 EL 异常，而是交给节点“审批人为空”策略处理。

### 验证说明

- 已完成策略枚举、前端配置、BPMN 转换器、运行时解析器和身份适配器的静态对照，并通过 `git diff --check`；未执行 Maven 编译、前端构建或运行时组织数据测试，遵循功能批次统一验证约定。

## 2026-09-11：清理流程实例时兼容挂起任务

### 本次更新

- 流程模型“清理”遇到挂起中的运行实例时保持挂起状态直接删除，避免恢复实例触发自动审批或定时器；活动取消监听阶段会跳过挂起任务变量补写。
- 清理运行实例和历史实例后同步删除抄送记录，避免审批中心残留已清理流程的抄送数据。
- 活动取消监听器继续对并发删除/挂起竞态保持幂等：展示用的取消变量写入失败不会阻断主流程清理事务。
- 自动通过、自动拒绝、空审批人处理和发起人同人处理在异步回调再次执行时会先跳过挂起任务，避免清理事务之外的回调继续向已挂起任务写变量。
- 超时定时器回调同样跳过挂起任务，避免在清理/暂停与定时器并发时写评论或再次完成任务。
- 退回、撤回和拒绝结束流程时同样跳过挂起任务的本地变量写入，避免在并行/会签任务被挂起的窗口内触发同类异常。

### 验证说明

- 已完成清理控制器→模型服务→Flowable 删除 API→活动取消监听器的静态链路检查，并通过 `git diff --check`；未执行 Maven 编译、数据库清理或服务重启，遵循当前统一验证约定。

### 后续目标

1. 在统一打包批次验证挂起实例、会签剩余任务和抄送记录的清理结果。
2. 继续补齐退回/撤回后的历史时间线和边界事件幂等记录。

## 2026-09-11：触发器节点补齐条件与响应闭环

### 本次更新

- 更新/删除表单触发器现在按目标版 `formSettings` 逐项判断条件；支持表达式条件以及条件组的且/或、规则的且/或和等于/不等于/比较/包含运算，只有命中的设置才会写入或移除流程变量。
- 同步 HTTP 触发器支持 `response` 映射：兼容响应体的 `data`、`result` 或根对象，将配置的响应字段写回流程变量；HTTP 回调类型仍只发送请求并等待生成的 ReceiveTask，不会误处理回调响应。
- 保留受保护变量前缀拦截，触发器不能覆盖流程内部状态变量。
- 简易设计器分支的 `conditionGroups` 规则现在也可转换成 Flowable 条件表达式，兼容且/或、数值/布尔/字符串比较以及包含运算；原有直接填写的 `conditionExpression` 继续优先兼容。

### 验证说明

- 已完成目标版 `BpmFormUpdateTrigger`、`BpmFormDeleteTrigger`、`BpmHttpCallbackTrigger` 与本项目委托的静态对照，并通过 `git diff --check`；未执行 Maven 编译、HTTP 联调或服务重启，遵循当前统一验证约定。

### 后续目标

1. 将触发器 JSON 配置从高级输入继续收敛到简易设计器的可视化参数面板。
2. 在统一运行批次验证 HTTP 响应映射、回调等待和表单条件的实际流程流转。

## 2026-09-11：打印模板兼容目标版占位符

### 本次更新

- 打印预览现在解析目标版 WangEditor `mention` 节点（`data-w-e-type="mention"`），支持流程名称、流程编号、发起人、部门、时间、状态、打印人及表单字段替换。
- 支持目标版 `data-w-e-type="process-record"` 占位符，预览时生成流程记录表；原有 `${field}` 模板占位符继续兼容。
- 保留模板清洗和 HTML 转义策略，替换值不会执行模板中的脚本或事件属性。

### 验证说明

- 已完成目标版 `process-print.vue` 与当前 `ProcessPrintModal.vue` 的模板数据映射静态对照，并通过 `git diff --check`；未执行前端构建或浏览器打印验证，遵循当前统一验证约定。

### 后续目标

1. 在统一验证批次确认 mention、流程记录和表单字段在真实模板中的打印效果。
2. 继续补齐签名图片、打印模板和流程报表的端到端联动。

## 2026-09-11：简易设计器触发器可视化配置

### 本次更新

- 触发器节点的 HTTP 请求类型新增请求头、请求体的可视化键值配置，并支持把响应字段映射回流程变量；配置结构与后端 `httpRequestSetting` 保持一致。
- 更新表单数据、删除表单数据类型保留 JSON 高级配置入口，支持多条 `formSettings` 及条件组，输入中的无效 JSON 只保留草稿，不会覆盖上一次有效配置。
- 切换触发器类型时自动补齐所需配置数组并移除不适用配置，避免保存后生成互相冲突的 BPMN 扩展属性。

### 验证说明

- 已完成简易设计器组件、HTTP 参数组件、响应映射组件与后端触发器委托字段的静态调用链检查，并通过 `git diff --check`；未执行前端构建、Maven 编译或浏览器联调，遵循功能批次统一验证约定。

### 后续目标

1. 在统一验证批次确认四类触发器配置可保存、发布并按条件运行。
2. 将表单触发设置进一步拆分为可视化条件组和字段编辑器，减少 JSON 输入。

## 2026-09-11：子流程运行事件纳入主流程时间线

### 本次更新

- 审批详情的运行事件查询会沿 `superProcessInstanceId` 递归收集调用活动创建的子流程实例。
- 子流程中的开始、结束、网关、服务任务、接收任务和边界事件等非用户活动，按实际开始时间合并到主流程时间线；原有用户任务记录仍由任务列表展示。
- 对重复/循环调用实例做流程实例 ID 去重，避免同一子流程事件在时间线中重复出现。

### 验证说明

- 已完成审批详情服务、Flowable 历史流程实例关系和前端运行事件展示的静态链路检查，并通过 `git diff --check`；未执行 Maven 编译、数据库查询或端到端测试，遵循功能批次统一验证约定。

### 后续目标

1. 将子流程用户任务的处理人、审批意见和退回关系也汇入统一时间线模型。
2. 在统一运行验证批次覆盖并行分支、循环退回和子流程嵌套场景。

## 2026-09-11：清理挂起流程时跳过取消变量补写

### 本次更新

- 活动取消监听器在收到挂起流程实例的取消事件时直接返回，只保留 Flowable 的删除动作，不再向挂起任务补写状态/原因变量。
- 继续保留任务级挂起判断和并发异常兜底，兼容清理过程中实例状态切换与任务实体删除的竞态。

### 验证说明

- 已完成清理入口、运行实例删除 API 和活动取消监听器的静态链路检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试，遵循当前统一验证约定。

### 后续目标

1. 在下一次统一后端打包后清理一个挂起实例，确认运行实例、历史实例和抄送数据均被删除。
2. 若仍存在异常，依据后端完整堆栈定位是否为外部 BPMN 监听器写变量。

## 2026-09-11：简易设计器表单触发器可视化配置

### 本次更新

- 新增 `TriggerFormSettings.vue`，将更新表单数据、删除表单数据从整段 JSON 输入改为卡片式配置。
- 支持添加/删除多条表单设置、选择执行条件、编辑表达式条件，以及按已关联表单字段配置修改值或删除字段。
- 条件组仍提供 JSON 高级入口，保留目标版 `conditionGroups` 结构和已有模型的兼容能力；无效草稿不会覆盖上次有效配置。
- 切换触发器类型时清理不适用的更新字段/删除字段，避免生成冲突的 `formSettings`。

### 验证说明

- 已完成简易设计器、表单字段属性和后端 `formSettings` 委托字段的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建，遵循当前统一验证约定。

### 后续目标

1. 将条件组 JSON 进一步替换为字段、运算符和条件组的可视化编辑器。
2. 在统一前端验证批次确认表单触发器保存、发布和运行后的字段更新/删除结果。

## 2026-09-11：子流程用户任务纳入运行时间线

### 本次更新

- 审批详情时间线继续保留主流程非用户活动，同时追加调用活动子流程中的用户任务。
- 子流程用户任务补充历史任务 ID、办理人、办理人名称、处理结果和处理意见，详情页可直接查看其审批上下文。
- 主流程用户任务仍由“审批进度”区域展示，避免同一任务在两个区域重复出现。

### 验证说明

- 已完成历史活动、历史任务、时间线 VO 和详情页渲染的静态链路检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试，遵循当前统一验证约定。

### 后续目标

1. 将子流程用户任务的签名、附件和退回关系也映射到时间线。
2. 在统一运行验证批次覆盖嵌套子流程、并行分支和循环退回的时间顺序。

## 2026-09-11：表单触发器条件组可视化编辑

### 本次更新

- 新增 `TriggerConditionGroups.vue`，支持条件组之间以及组内规则的且/或切换。
- 支持从已关联表单字段选择左值、选择等于/比较/包含运算符、填写右值，并支持添加/删除条件组和规则。
- 保留高级 JSON 折叠入口，已有 `conditionGroups` 数据可以继续编辑，且无效 JSON 不会覆盖有效配置。

### 验证说明

- 已完成条件组组件、表单触发设置组件、简易设计器模型和后端条件表达式转换器的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 增加条件规则必填校验，阻止未选择字段或比较值的设置直接发布。
2. 在统一前端验证批次确认条件组生成的 Flowable 表达式与目标版运行结果一致。

## 2026-09-11：简易流程触发器发布前校验

### 本次更新

- 简易流程保存时递归检查触发器配置：HTTP 请求地址、表单设置数量、修改字段、删除字段和条件组规则。
- 条件表达式、字段映射或条件组存在空值时在流程设计步骤直接提示，不再把无法运行的触发器配置发布到 Flowable。
- 校验覆盖主链、条件分支和并行分支中的触发器节点，保持 JSON 高级编辑与可视化编辑使用同一套规则。

### 验证说明

- 已完成 `ModelWizard` 校验、`SimpleProcessDesigner` 数据结构和后端触发器委托的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 将同类必填校验扩展到审批人、办理人和候选人策略，减少发布后空审批人情况。
2. 在统一前端验证批次确认合法触发器能保存、发布并完成实际流程流转。

## 2026-09-11：简易流程审批配置发布前校验

### 本次更新

- 简易流程保存时校验人工审批/办理节点的候选策略参数、发起人部门层级、会签比例、审批人为空处理和拒绝退回节点。
- 自动通过、自动拒绝、发起人自选、审批人自选等不需要固定候选参数的策略保持兼容，不会被误判为缺少配置。
- 校验与触发器校验共用流程设计步骤，覆盖主链和条件/并行分支节点。

### 验证说明

- 已完成 `ModelWizard` 保存校验、简易设计器节点结构和后端候选人解析入口的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 增加用户组、表单用户字段和部门字段的类型级校验，避免候选参数类型不匹配。
2. 在统一验证批次覆盖空审批人策略和连续部门负责人策略的真实租户数据。

## 2026-09-11：清理挂起流程的取消事件竞态保护

### 本次更新

- 新增 `WorkflowProcessCleanupContext`，在清理流程实例的删除事务期间标记当前线程。
- 活动取消监听器检测清理上下文后不再给任务补写“已取消”等本地变量，避免 Flowable 在挂起任务上抛出 `Cannot add variables to a suspended Task`。
- 正常流程流转中的会签/或签取消事件仍保留原有任务状态补写逻辑；清理完成后上下文始终通过 `finally` 释放。

### 验证说明

- 已完成清理服务、Flowable 活动取消监听器和线程上下文的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试，遵循当前统一验证约定。

### 后续目标

1. 下一次统一后端打包并重启后，清理一个挂起实例，确认运行实例、历史实例及抄送数据均可删除。

## 2026-09-11：审批节点字段、按钮与监听器发布校验

### 本次更新

- 简易设计器保存时校验字段权限 JSON 的字段名与权限值，兼容 `READONLY/EDITABLE/HIDE` 别名。
- 校验操作按钮编号及配置结构；人工审批节点至少保留“通过”或“拒绝”之一，避免保存后任务无可用办理入口。
- 校验创建任务、指派任务、完成任务 HTTP 监听器地址，以及高级任务/执行监听器 JSON 的事件和实现字段。
- 校验仅作用于简易设计器保存流程，BPMN 高级配置仍由后端模型转换器负责兼容与校验。

### 验证说明

- 已完成 `ModelWizard`、`ApprovalNodeConfig`、简易模型转换器和审批运行服务的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 在统一运行验证批次确认字段权限确实限制可写字段，按钮权限在后端同步生效，监听器请求能携带流程上下文。

## 2026-09-11：简易设计器审批按钮配置兼容

### 本次更新

- 简易设计器打开历史模型时，将数组、JSON 字符串或缺失的 `buttonsSetting` 统一归一化为六种标准操作按钮。
- 兼容旧模型仅保存 `enabledButtons` 的情况，并在节点配置面板中保持按钮开关与按钮编号同步，避免字符串配置导致页面遍历异常。

### 验证说明

- 已完成 `SimpleProcessDesigner` 按钮配置归一化与 `ApprovalNodeConfig` 双向绑定的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 在统一运行验证批次确认旧模型升级后按钮显示、保存和后端权限拦截均保持一致。

## 2026-09-11：取消流程删除竞态复用清理保护

### 本次更新

- 管理员/发起人取消运行中流程时，同样在 Flowable 删除实例阶段启用清理上下文。
- 取消流程与模型“清理”共用挂起任务保护，避免取消挂起实例时活动取消监听器再次写入任务变量。

### 验证说明

- 已完成取消服务、清理上下文和活动取消监听器的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一重启后分别验证“清理模型实例”和“取消单个实例”在挂起、普通运行两种状态下均可完成。

## 2026-09-11：候选人策略参数类型校验

### 本次更新

- 简易设计器发布前校验指定用户、角色、部门、岗位和用户组参数，避免空值或非数字用户组编号进入运行时。
- 校验连续多级部门负责人、表单内部门负责人所需的“参数|层级”格式，以及发起人部门负责人层级必须为正整数。
- 校验表单内用户字段、表单内部门字段是否存在于已选流程表单；业务表单无法预取字段时仍保留字段名格式校验。
- 校验流程表达式策略使用 `${...}` 形式，减少候选人解析阶段才暴露配置错误的情况。

### 验证说明

- 已完成 `ModelWizard` 候选策略校验与 `WorkflowCandidateResolver` 参数约定的静态对照，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 下一次统一验证批次使用真实用户、部门、岗位、用户组和表单字段分别启动流程，确认每种策略都能创建待办。

## 2026-09-11：打印模板流程编号对齐业务标识

### 本次更新

- 打印预览默认布局及自定义模板的“流程编号/processNum”占位符，统一优先展示流程实例业务标识；业务标识为空时才回退到 Flowable 实例 ID。
- 保持已有流程记录、表单变量和签名展示逻辑不变，避免历史实例没有业务标识时出现空白编号。

### 验证说明

- 已完成 `ProcessPrintModal` 与 `WorkflowPrintDataVO` 字段映射的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行前端构建。

### 后续目标

1. 下一次统一运行验证批次确认内置布局和自定义模板在运行中、已完成、已取消实例上均能打印业务标识及流程记录。

## 2026-09-11：独立启动器部门负责人解析修正

### 本次更新

- 独立工作流启动器按传入用户 ID 查询其部门映射，再计算发起人部门负责人和连续多级部门负责人。
- 未配置其他用户映射时不再错误回退到启动器当前用户的默认部门，避免候选人策略把负责人分配到错误组织。

### 验证说明

- 已完成 `StandaloneWorkflowIdentityAdapter` 与候选人解析入口的静态对照，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 下一次独立服务验证批次补充多用户、多部门和父部门映射，确认连续负责人策略生成完整候选集合。

## 2026-09-11：独立启动器支持多负责人部门映射

### 本次更新

- 独立启动器新增 `department-leader-users` 多值映射，与原有 `department-leaders` 单值配置合并去重。
- `ISysBaseAPI` 兼容代理和候选人适配器均返回完整负责人集合，使连续部门负责人、会签和或签策略可以覆盖同一部门的多个负责人。

### 验证说明

- 已完成独立启动器属性、身份适配器和兼容代理的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续对照芋道 BPM 的候选策略编号和参数约定，补充策略边界（空部门、跨租户、失效用户）运行时保护。

## 2026-09-11：用户组候选策略支持独立服务映射

### 本次更新

- 为候选人身份适配器增加可选的用户组查询契约；JeecgBoot 单体继续使用工作流用户组表，保持原有数据来源不变。
- 独立启动器新增 `user-group-users` 配置，可在没有 system-biz 或用户组数据库表的部署中直接解析用户组候选人。
- 组映射结果仍经过活动用户过滤和去重，兼容会签、或签及普通单实例任务。

### 验证说明

- 已完成候选解析器、身份适配器、独立启动器代理和示例配置的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续补充策略边界保护，并在统一验证批次覆盖数据库用户组与独立配置用户组两种来源。

## 2026-09-11：兼容审批人为空候选策略编号

### 本次更新

- 对照芋道 `BpmTaskCandidateStrategyEnum` 增加策略编号 `1` 的解析兼容。
- 编号 1 不再要求候选参数，运行时按节点“审批人为空”配置解析指定人员或流程管理员；数据库和独立启动器两种身份来源均沿用同一回退规则。
- 简易模型转换器允许旧模型保留该编号，并继续通过多实例/单实例的统一候选解析入口处理。

### 验证说明

- 已完成候选解析器、简易模型转换器与芋道策略枚举的静态对照，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续补充策略边界保护，并在统一验证批次覆盖策略 1 与其他候选策略的真实任务创建结果。

## 2026-09-11：表达式候选策略兼容 Flowable 表达式语法

### 本次更新

- 简易设计器不再强制流程表达式必须是单一的 `${...}` 形式，兼容 Flowable 的 `#{...}`、注册函数和组合表达式。
- 仍要求表达式非空；具体语法由 Flowable `ExpressionManager` 在发布或运行时解析，避免前端校验规则先于引擎限制可用表达式。

### 验证说明

- 已完成芋道 `BpmTaskCandidateExpressionStrategy` 与当前候选人解析入口的静态对照，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中分别使用 `${...}`、`#{...}` 和表单变量表达式创建任务，确认表达式解析结果与活动用户过滤一致。

## 2026-09-11：自动审批节点跳过候选人计算

### 本次更新

- 候选人解析入口与芋道 `BpmTaskCandidateInvoker` 对齐：`approveType=2/3` 的自动通过、自动拒绝节点直接返回空候选集合。
- 自动节点继续由任务创建监听器完成，避免同时建立普通待办后再自动处理，减少重复任务和候选人误判。

### 验证说明

- 已完成候选人解析器与自动任务监听器的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖自动通过、自动拒绝与人工节点混合的串行、会签流程，确认自动节点不产生待办且后续节点能正常进入。

## 2026-09-11：自选审批人增加后端必选校验

### 本次更新

- 流程发起和消息启动时，后端按实际可达路径检查“发起人自选”节点，缺少审批人直接返回明确错误，避免流程启动后卡在空候选任务。
- 审批完成前，后端按下一步可达用户任务检查“审批人自选”节点，要求请求携带对应节点的审批人；已保存的选择会与本次选择合并。
- 用户 ID 仍经过统一的活动用户校验，和普通候选人策略保持一致。

### 验证说明

- 已完成启动、审批完成、候选解析和前端选择变量的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖串行、条件分支和会签下的发起人自选/审批人自选，确认缺少选择时不会生成不可办理任务。

## 2026-09-11：审批结果提前纳入下一节点预测

### 本次更新

- 审批完成时，下一节点可达性和审批人自选校验上下文提前注入本次 `approved` 结果。
- 条件分支或候选人表达式依赖 `approved` 时，预测路径与 Flowable 完成任务后的实际变量保持一致。

### 验证说明

- 已完成审批完成、条件分支导航和自选审批人校验的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖通过/拒绝条件分支，确认下一节点预测、候选人校验和实际任务创建走同一分支。

## 2026-09-11：运行时自选审批人缺失保护

### 本次更新

- 候选人解析器在真实运行任务中对策略 34/35 强制检查节点选择变量及节点对应的用户列表。
- 预测接口仍允许空选择以展示待选节点；运行时缺失选择会返回明确业务错误，不再静默生成空待办。

### 验证说明

- 已完成候选解析器运行态/预测态分支的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖直接启动、消息启动、审批完成和回退后再次进入自选节点的缺失选择场景。

## 2026-09-11：审批完成时区分两类自选审批人变量

### 本次更新

- 审批人选择面板同时展示“审批人自选”(34) 和尚未在发起阶段配置的“发起人自选”(35) 下一节点。
- 后端按目标节点策略分别写入 `PROCESS_APPROVE_USER_SELECT_ASSIGNEES` 与 `PROCESS_START_USER_SELECT_ASSIGNEES`，并保留已有选择不被覆盖。
- 审批完成前同时校验两类自选节点，避免请求只写入错误的变量后形成空候选任务。

### 验证说明

- 已完成前端选择面板、审批请求、流程变量写入和候选解析的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖审批人自选、发起人自选单独及混合出现的串行/并行路径，确认变量命名和任务候选人一致。

## 2026-09-11：流程时间线区分取消活动

### 本次更新

- 时间线事件读取 Flowable 历史活动的 `deleteReason`，被退回、撤回、并行完成条件取消或流程清理的活动标记为取消状态。
- 正常结束的网关、服务任务、子流程和调用活动继续显示为完成，运行中的活动保持运行状态。

### 验证说明

- 已完成历史活动状态映射和子流程时间线聚合的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖并行分支完成条件、退回、撤回和子流程超时，核对时间线的取消原因与活动状态。

## 2026-09-11：子流程超时删除复用清理保护

### 本次更新

- 子流程超时边界事件删除子流程时复用流程清理上下文，避免取消监听器向挂起任务写展示变量导致超时处理回滚。
- 保持边界事件的原有行为：只删除与当前调用活动对应的子流程，不影响其他并行子流程。

### 验证说明

- 已完成超时监听器、子流程查询和取消事件监听器的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖活动超时、延迟等待超时和子流程超时三类边界事件，并核对挂起实例的清理结果。

## 2026-09-11：触发器 HTTP 请求补充租户上下文

### 本次更新

- 简易设计器的 HTTP 请求/回调触发器自动携带 `X-Tenant-Id`，优先使用 Flowable 执行租户，缺失时回退到流程变量中的租户标识。
- 保留原有表单参数、回调节点编号和返回值变量映射，避免跨租户业务接口无法识别调用来源。

### 验证说明

- 已完成触发器委托、租户变量和 Jeecg 请求头常量的静态调用链检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中用不同租户分别运行同步 HTTP、HTTP 回调和表单更新触发器，核对请求头及流程变量隔离。

## 2026-09-11：清理流程挂起任务竞态修复

### 本次更新

- 清理流程实例时，Flowable 可能在 `ACTIVITY_CANCELLED` 事件中先移除 runtime 实例，再携带旧的挂起任务对象回调审批人监听器。监听器现在把“实例已不可查询”与“实例已挂起”统一视为清理窗口，跳过任务本地变量补写，避免 `Cannot add variables to a suspended Task` 阻断模型清理。
- 正常的多实例/并行节点取消仍会在流程实例可查询且未挂起时补写取消状态；流程删除后的历史状态以 Flowable 的删除原因和历史活动记录为准。

### 验证说明

- 未重新打包、未重启服务、未执行端到端测试；仅完成代码静态检查与 `git diff --check`。

## 2026-09-11：BPMN 设计器候选人策略运行时接入

### 本次更新

- BPMN 原生用户任务创建后，如果任务没有 `assignee`，现在会读取 `candidateStrategy/candidateParam` 扩展配置，复用 `WorkflowCandidateResolver` 解析角色、部门、岗位、用户组、发起人及表单字段等候选人策略，并设置实际待办负责人。
- 多实例用户任务优先使用 Flowable 的 `elementVariable` 当前实例负责人，不再把整个会签/或签候选集合压缩成一个随机负责人，保持每个实例独立分配。
- 简易设计器原有的 assignee 表达式路径保持不变；BPMN 设计器和简易设计器现在共享候选人数据适配边界。

### 验证说明

- 已完成任务创建监听器、候选人解析器和多实例变量路径的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中分别覆盖 BPMN 原生单实例、会签、或签和连续部门负责人节点，确认候选人策略都能生成正确待办。

## 2026-09-11：BPMN 设计器候选人配置面板对齐

### 本次更新

- BPMN 设计器不再要求所有候选参数手写编号；指定用户、角色、部门、岗位改用与简易设计器一致的选择组件。
- 连续部门负责人、发起人部门负责人和表单部门负责人分别提供部门层级/字段配置，并统一保存为后端解析的 `参数|层级` 格式。
- 发起人自选、审批人自选、发起人本人、表单用户字段、用户组和流程表达式显示对应的专用配置提示，切换策略时同步清理旧参数。

### 验证说明

- 已完成 BPMN 面板模板、候选参数序列化和后端 `WorkflowCandidateResolver` 参数格式的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续对齐 BPMN 设计器的字段权限、按钮权限、监听器模板和边界事件配置交互，并在统一验证批次中验证保存后重新打开配置不丢失。

## 2026-09-11：BPMN 原生多实例配置闭环

### 本次更新

- BPMN 设计器的“依次审批、会签、或签、随机一人”不再只保存扩展字段；切换配置时会同步写入 Flowable 原生 `MultiInstanceLoopCharacteristics`。
- 多实例集合统一使用 `${workflowCandidateResolver.resolveAll(execution)}`，元素变量统一为节点专属的 `_workflowTaskAssignee_<节点ID>`，与后端任务创建监听器的负责人分配逻辑一致。
- 会签按比例生成整数计数条件，或签生成首个完成即结束条件，依次审批生成顺序执行并全部完成条件；随机一人会移除多实例配置。
- 增补 Flowable moddle 的 `collection` 与 `elementVariable` 属性声明，确保 BPMN XML 保存后重新打开仍能保留集合配置。

### 验证说明

- 已完成 BPMN 模板、moddle 扩展和后端变量命名的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续补齐 BPMN 设计器的消息/信号、边界事件和监听器运行验证，随后统一验证原生 BPMN 的会签、或签和依次审批计数。

## 2026-09-11：BPMN 监听器 JSON 运行闭环

### 本次更新

- BPMN 设计器中的任务监听器 JSON 和执行监听器 JSON 现在会生成标准 `flowable:TaskListener` / `flowable:ExecutionListener` 元素，不再只保存 Flowable 无法执行的自定义文本扩展。
- 重新打开 BPMN 模型时会从标准监听器元素回显事件、实现类型和实现值；模板选择与手工 JSON 配置可以共存。

### 验证说明

- 已完成监听器元素生成、回显和现有模板合并逻辑的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证 class、expression、delegateExpression 三种监听器实现，覆盖任务创建、分配、完成和流程 start/end 事件。

## 2026-09-11：清理流程按实例优先顺序执行

### 本次更新

- 流程模型“清理”现在在同一清理上下文中，按租户先删除该模型的运行实例，再删除历史实例、流程副本和可能遗留的任务；与芋道目标版的实例优先顺序保持一致。
- 对清理期间任务已经被并发删除或挂起的情况保持幂等，避免任务取消监听器再次写入任务变量导致 `Cannot add variables to a suspended Task`。
- 流程完成监听器在清理上下文中不再补写完成状态，避免删除动作与完成事件交错时访问已失效的运行实例。

### 验证说明

- 已完成清理入口与 Flowable 任务删除顺序的静态检查；未重新打包、重启服务或执行端到端测试。

## 2026-09-11：BPMN 边界消息/信号/错误事件导出闭环

### 本次更新

- BPMN 设计器配置边界错误、消息或信号事件时，会把对应的 `bpmn:Error`、`bpmn:Message`、`bpmn:Signal` 注册到 Definitions 根元素，并通过事件定义引用它们。
- 修改已有边界事件时复用稳定的根元素编号，避免重复创建同名引用；导出的 BPMN XML 可被 Flowable 解析为真正的事件订阅/边界事件，而不是只保存在画布内存中。
- 保留原有定时器边界事件的超时处理扩展；消息/信号边界事件继续由 `/bpm/task/trigger-event` 触发。

### 验证说明

- 已完成边界事件创建、根元素引用和重新编辑路径的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在统一验证批次中覆盖消息、信号、定时器和错误边界事件的导出、部署、触发与流程时间线记录。

## 2026-09-11：执行监听器 class 契约修正

### 本次更新

- 流程监听器模板的 `execution + class` 校验改为要求实现 Flowable `ExecutionListener`，与 BPMN `flowable:ExecutionListener` 的实际运行契约一致。
- 任务监听器仍要求实现 `TaskListener`；表达式和 `delegateExpression` 继续要求 `${...}` 格式。
- 修复了“模板可以保存，但发布/运行时才因接口类型错误失败”的延迟错误。

### 验证说明

- 已完成监听器类型校验和异常提示的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证流程级 start/end、任务 create/assignment/complete 三类监听器的 class、expression、delegateExpression 实际触发结果。

## 2026-09-11：BPMN 单实例随机审批人语义对齐

### 本次更新

- BPMN 原生用户任务使用“随机挑选一人审批”时，不再由任务分配监听器取候选集合第一人。
- 新增无 `DelegateExecution` 场景下的随机候选解析入口，任务创建监听器与简易设计器的 `resolveOne(execution)` 现在共享同一套候选人、租户、空候选回退和发起人处理逻辑。
- 会签、或签和顺序多实例仍由 `elementVariable` 为每个实例分配独立负责人，不受此次单实例随机逻辑影响。

### 验证说明

- 已完成候选解析器调用路径、租户参数传递和静态引用检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证随机单实例、会签、或签和连续部门负责人四类节点的实际待办分配与审批结果。

## 2026-09-14：BPMN 超时提醒次数配置对齐

### 本次更新

- BPMN 设计器的审批超时面板新增“提醒次数”，与简易设计器的超时处理配置保持一致。
- 加载已有模型时会从定时器的 `timeCycle` 回显重复次数；保存或修改提醒类型、间隔和次数时同步生成 `R次数/间隔` 的 Flowable 定时器表达式。
- 自动同意、自动拒绝仍使用一次性定时器，只有“自动提醒”且次数大于 1 时启用重复触发。

### 验证说明

- 已完成定时器表达式生成、回显和边界事件更新路径的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证提醒重复触发、超时自动同意/拒绝，以及边界事件后续连线对流程状态和时间线的影响。

## 2026-09-14：重复提醒定时器采用标准互斥表达式

### 本次更新

- 重复提醒现在只生成 `timeCycle=R次数/间隔`，一次性自动同意/拒绝只生成 `timeDuration`，避免同一个 BPMN 定时器同时出现互斥的时间属性。
- BPMN 设计器读取已有重复提醒时，会从 `timeCycle` 解析提醒次数和间隔并正确回显；简易设计器转换器也使用相同规则。
- Flowable 定时器每次触发仍由超时监听器写入提醒记录，达到重复次数后由引擎自动结束定时器。

### 验证说明

- 已完成 BPMN/简易设计器定时器属性生成与回显的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证重复提醒次数、超时自动处理和边界事件后续连线的实际运行结果。

## 2026-09-14：抄送节点兼容无租户流程

### 本次更新

- 抄送节点运行时读取不到 Flowable 租户编号时，统一使用 Jeecg 默认租户 `0`，再执行历史实例校验和抄送记录落库。
- 避免非租户流程在 `Long.parseLong(null)` 处失败；有租户部署仍保留原租户隔离行为。

### 验证说明

- 已完成抄送节点租户参数归一化和数据落库路径的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证有租户、无租户及独立工作流服务三种运行场景下的抄送记录、查询权限和流程详情展示。

## 2026-09-14：取消流程兼容挂起任务竞态

### 本次更新

- 取消流程在补写任务取消状态前增加 Flowable 挂起任务异常保护。
- 模型清理现在先删除运行中的流程实例，再清理历史数据和残留任务；挂起任务由流程实例删除统一清理，避免触发 Flowable 的挂起任务变量校验。
- 查询任务后若任务被并发挂起或删除，跳过展示变量写入并继续由 `deleteProcessInstance` 完成引擎清理，避免出现 `Cannot add variables to a suspended Task`。
- 非挂起任务异常仍会正常抛出，不隐藏真实的数据或权限错误。

### 验证说明

- 已完成取消/清理路径的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在用户授权重新启动后，用一个挂起中的流程实例验证取消和模型清理都能成功完成，并确认历史记录及抄送数据的一致性。

## 2026-09-14：简易设计器子流程配置落到 Flowable

### 本次更新

- 简易设计器子流程节点现在保存“是否跳过发起人节点”、子流程发起人来源及空值兜底配置，并通过 CallActivity 输入参数传入 `PROCESS_SKIP_START_USER_NODE`。
- 子流程可配置固定数量、数字表单字段或多选表单字段多实例，并生成 Flowable 原生串行/并行循环和完成比例表达式。
- 新增 `workflowCallActivityListener`，在子流程启动前按主流程发起人、表单用户字段、子流程管理员或主流程管理员确定子流程发起人。
- 前端子流程面板补充上述开关、来源、超时和多实例设置，旧模型缺少字段时自动使用兼容默认值。

### 验证说明

- 已完成子流程转换器、调用活动监听器和前端配置字段的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在重新启动环境后验证同步/异步子流程、三种多实例来源和四种发起人兜底路径的实际实例创建与父子流程时间线。

## 2026-09-14：子流程详情权限覆盖父子实例

### 本次更新

- 流程详情的查看权限现在递归收集调用活动产生的子流程实例。
- 子流程办理人、候选人和历史办理人均可查看父流程详情中的评论、审批记录和聚合时间线；原有发起人及流程管理员权限保持不变。
- 权限查询仍按当前租户过滤，未扩大到其他租户或无关流程实例。

### 验证说明

- 已完成权限检查、子流程历史任务查询和时间线实例收集的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证父流程办理人、子流程办理人、候选人和管理员四类账号的详情/评论访问边界。

## 2026-09-14：子流程数字/多选表单多实例表达式修正

### 本次更新

- 子流程多实例来源为“数字表单”时，转换器现在生成 Flowable `loopCardinality` 数值表达式；不再把数字字段错误地当作集合传给 `inputDataItem`。
- 来源为“多选表单”时生成标准 `${字段}` 集合表达式，并继续保留串行/并行和完成比例设置。
- 已兼容用户填写 `${字段}` 或 `#{字段}` 的表达式格式，避免重复包裹表达式。

### 验证说明

- 已完成子流程多实例来源类型、Flowable 属性互斥关系和表达式生成的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证固定数量、数字表单和多选表单三种子流程多实例在同步/异步调用下的实例数量、完成比例和父子时间线。

## 2026-09-14：发起人节点转换为真实用户任务

### 本次更新

- 简易设计器的“发起人”节点现在导出为真实 Flowable `UserTask`，候选人固定为流程发起人，并沿用发起人相同处理规则。
- 父流程保持默认自动跳过；子流程通过调用活动输入变量 `PROCESS_SKIP_START_USER_NODE` 控制自动跳过或保留发起人待办。
- 流程预览和详情时间线避免同时展示合成发起人节点与真实 UserTask，旧模型未包含该节点时仍保留兼容的合成节点。

### 验证说明

- 已完成简易模型转换、发起人任务监听器和预览/详情节点去重的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 统一验证父流程、子流程跳过发起人和保留发起人三种场景的待办生成、自动完成和历史时间线。

## 2026-09-14：清理前恢复挂起流程实例

### 本次更新

- 模型清理发现运行中的流程实例处于挂起状态时，先调用 Flowable 激活实例，再执行删除。
- 清理上下文仍会抑制取消事件的展示变量补写，避免挂起任务在删除竞态中触发 `Cannot add variables to a suspended Task`。
- 新增监听器单元测试覆盖挂起任务不写入取消变量的场景。

### 验证说明

- 已完成清理服务和取消监听器的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 在用户授权重新启动后，清理一个包含挂起父/子流程的模型，确认运行实例、历史实例及抄送数据均被一致删除。

## 2026-09-14：BPMN 设计器节点面板显示修正

### 本次更新

- BPMN 设计器继续使用 `bpmn-js` 原生建模器，左侧节点面板支持拖入开始事件、用户任务、网关、结束事件等标准节点。
- 将 `bpmn-js` 的 diagram/palette 样式从 Vue `scoped` 样式移到全局样式；建模器运行时创建的 DOM 不带 Vue 作用域属性，之前因此会出现画布存在但节点工具栏不可见或样式异常。
- 新建 BPMN 模型在没有 XML 时自动创建初始图，并适配画布，打开设计器即可看到可拖拽的节点面板。

### 验证说明

- 已完成组件源码和样式作用域的静态检查，并通过 `git diff --check`；按要求未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续补齐 BPMN 设计器的完整配置面板、Flowable 扩展节点运行语义和设计保存后的实例验证。

## 2026-09-14：人工跳过任务操作

### 本次更新

- 后端新增 `/bpm/task/skip`，沿用当前办理人、按钮权限、租户和候选任务校验，并记录跳过原因、附件及评论。
- 简易设计器与流程详情页补充“跳过”按钮配置和操作入口；子任务、委派任务和多实例父任务按现有任务关系处理，避免跳过未完成的子任务集合。

### 验证说明

- 已完成接口、服务和前端调用链的静态检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

## 2026-09-14：BPMN 审批节点操作按钮配置面板

### 本次更新

- BPMN 设计器的用户任务节点新增“操作按钮”多选面板，支持通过、拒绝、转办、委派、加签、退回、抄送和跳过。
- 多选结果写入 Flowable `enabledButtons` 扩展属性，与办理页后端权限校验使用同一份配置；原有“操作按钮高级 JSON”仍保留用于自定义显示名称等高级场景。
- 读取旧 BPMN XML 时没有该扩展属性会使用兼容默认值，不影响已有流程模型。

### 验证说明

- Vite 开发服务已成功返回更新后的 `BpmnDesigner.vue`（HTTP 200），并确认 `enabledButtons` 与按钮选项已进入编译结果；通过 `git diff --check`，未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 将字段权限、监听器和网关/事件配置从 JSON 高级入口继续补齐为可视化配置，并验证保存后的 BPMN XML 与运行时行为一致。

## 2026-09-14：简易设计器新增按钮校验同步

### 本次更新

- 修正模型向导对审批操作按钮的白名单，允许抄送（7）和跳过（8），与简易设计器、BPMN 设计器以及办理页后端默认按钮保持一致。
- 保留“通过/拒绝”至少启用一个的校验，避免审批节点保存后没有可执行操作。

### 验证说明

- 已完成前端按钮配置、向导校验和后端按钮编号的静态交叉检查，并通过 `git diff --check`；未重新打包、重启服务或执行端到端测试。

### 后续目标

1. 继续将字段权限和监听器配置从高级 JSON 入口补齐为可视化编辑，并覆盖 BPMN 与简易设计器的保存兼容性。
## 2026-09-14：BPMN 设计器节点工具栏和配置面板补齐

- 对照 `yudao-ui-admin-vben/apps/web-antd/src/views/bpm/components/bpmn-process-designer` 的 `CustomPalette` 和 `PropertiesPanel`，新增高优先级 BPMN 工具栏提供器 `jeecgboot-vue3/src/views/bpm/components/BpmnPalette.ts`。
- 工具栏现在提供中文悬停提示，并支持开始/中间/结束事件、排他/并行/包容/事件网关、用户任务、服务任务、脚本任务、接收任务、调用子流程、扩展子流程、数据对象/存储和分组；扩展子流程拖入画布时自动带开始事件。
- `BpmnDesigner.vue` 注册该工具栏并为动态 palette entry 同步 `title` 属性，因此鼠标停留会显示提示；同时保留原有 Flowable 服务任务、调用子流程配置。
- 右侧面板补充元素文档、异步前/异步后/独占执行设置，以及扩展子流程的展开/折叠和事件触发配置；这些设置会回写 BPMN XML。
- 服务任务 HTTP 配置补充禁止重定向、保存响应变量、瞬时变量、JSON 响应变量等 Flowable 字段；调用子流程补充调用标识类型（Key/流程定义 ID/消息）。
- 本次未重新打包或重启服务，仅通过运行中的 Vite 源码转换请求和 `git diff --check` 做静态验证。

下一步：继续按芋道面板逐项补齐字段权限可视化编辑、监听器配置表单和服务任务 HTTP 高级参数，并对新增节点进行一次最小浏览器交互验证。
## 2026-09-14：BPMN 字段权限与监听器配置可视化

- `BpmnDesigner.vue` 新增表单字段权限编辑器：根据当前流程表单字段生成字段列表，可批量设为只读/可编辑/隐藏，并继续保留高级 JSON 入口；保存时统一写入 `flowable:formFieldsPermission`，运行时读取为 `READ/WRITE/NONE` 权限映射。
- 新增任务监听器与执行监听器的结构化编辑行，可选择事件、实现类型（代理表达式/Java 类/表达式）、实现值并添加/删除；高级 JSON 与可视化编辑保持同步，仍使用现有 Flowable XML listener 写入逻辑。
- 新建/编辑向导和模型设计弹窗将已选流程表单字段传入 BPMN 设计器，避免字段权限界面只能依赖手写字段名。
- 本次继续未重新打包或重启服务；运行中的 Vite 源码转换请求对 `BpmnDesigner.vue`、`ModelWizard.vue`、模型页均返回 HTTP 200，`git diff --check` 无新增内容错误。

下一步：补齐流程表单绑定/流程级监听器的完整可视化面板，并继续核对退回、撤回、超时边界事件和多实例运行时的端到端语义。

## 2026-09-14：补充可交付的工作流数据库安装包

- 新增 `docs/sql/workflow-install-mysql.sql`，合并 Flowable 8.0.0 官方 MySQL common/engine/history 表结构与当前 Jeecg 工作流业务、菜单、角色权限增量 SQL。
- 新增 `docs/sql/README.md`，说明基础库与工作流包的导入顺序、PowerShell 命令以及 `ACT_GE_PROPERTY` 缺失的处理方式。
- 这次只整理 SQL 和交付说明，没有重新打包、重启服务或修改现有运行配置；合并包应在基础 JeecgBoot 数据库导入后执行一次。

下一步：让接收项目的人按 `docs/sql/README.md` 导入后启动服务，确认 Flowable 表、菜单权限和最小流程闭环均可用。

## 2026-09-14：修正简易设计器部门候选人参数

- 修正审批人/办理人配置面板在“部门成员”和“部门负责人”策略下的部门参数编码：这两种策略现在只保存部门 ID；“连续多级部门负责人”和“表单内部门负责人”才保存 `部门参数|层级`。
- 避免后端收到类似 `部门ID|1` 的无效部门编号，补齐候选人策略在界面选择与运行时解析之间的闭环。

验证说明：已完成前后端候选策略字段的静态交叉检查和 `git diff --check`；本轮未重新打包、重启或执行端到端测试。

下一步：补充候选人策略的自动化覆盖，继续核对会签/或签多实例的完成条件、空候选人和撤回/退回时间线。

## 2026-09-14：连续部门负责人层级遍历防循环

- 为“部门负责人指定层级”和“连续多级部门负责人”增加已访问部门集合，部门父级配置出现自引用或环形引用时会安全停止，不再无限遍历。
- 多父级部门仍按现有稳定顺序选取第一个未访问父级；已解析的负责人继续使用去重集合，避免重复候选人。

验证说明：已完成后端解析逻辑的静态检查并通过 `git diff --check`；本轮未重新打包、重启服务或执行端到端测试。

下一步：为全部候选人策略补充单元测试夹具，并继续核对会签/或签的多实例完成条件和空候选人处理。

## 2026-09-14：BPMN 节点中文显示与默认名称

- 新增 `BpmnTranslation.ts`，覆盖 bpmn-js 原生上下文菜单中的常见任务、网关、事件、子流程和连线英文标签，避免工具栏/替换菜单回退为英文。
- BPMN 工具栏新建节点直接写入中文默认名称；载入没有名称的历史节点时补写中文名称，办理记录也会继续使用该名称。
- 右侧属性面板的“节点类型”改为中文显示，内部仍保留原始 `bpmn:*` 类型用于配置判断和 XML 保存。

验证说明：运行中的 Vite 对 `BpmnDesigner.vue`、`BpmnPalette.ts` 和 `BpmnTranslation.ts` 均返回 HTTP 200，`git diff --check` 无新增内容错误；未重新打包、重启服务或执行浏览器端到端测试。

下一步：继续补齐 BPMN 事件/服务任务高级配置与运行时闭环，并对会签、或签及候选人策略做最小浏览器验证。

补充：bpmn-js 内部仍使用英文 `bpmn:*` 类型标识，这是 Flowable/BPMN XML 的技术字段，不会作为用户可见中文名称；本轮同时覆盖了非中断、取消、补偿、升级、链接等事件的内置菜单翻译。

## 2026-09-15：兼容历史 BPMN 默认英文名称

- 对历史 XML 中由 bpmn-js 自动写入的 `User task`、`Service task`、事件/网关等默认英文名称增加精确翻译；载入时只替换已知默认标签，不覆盖用户自行填写的英文业务名称。
- 工具栏、右键菜单和历史节点名称现在共用同一套翻译查找逻辑，避免新旧流程显示不一致。

验证说明：已完成源码静态检查和 `git diff --check`；按当前约定未重新打包、重启服务或执行页面端到端测试。

下一步：继续核对 BPMN 流程级表单绑定、监听器模板和高级事件配置的保存/运行闭环。

## 2026-09-15：流程级监听器模板与结构化配置保持同步

- 调整 `BpmnDesigner.vue` 的流程级执行监听器模板保存逻辑：选择模板时保留未被模板管理的手写监听器，避免误删高级配置。
- 模板选择后立即同步结构化监听器行和已选模板状态，后续编辑流程级监听器不会写回过期数据。
- 结构化编辑流程监听器后重新计算模板选中项，保证模板选择与 BPMN XML 中实际内容一致。

验证说明：完成源码静态检查并执行 `git diff --check`（无内容错误）；按当前约定未重新打包、重启服务或执行页面端到端测试。
下一步：继续核对 BPMN 高级节点（服务节点、子流程、边界事件）配置的保存与 Flowable 运行时映射。

## 2026-09-15：前后端开发服务已启动

- 后端以 `org.jeecg.JeecgSystemApplication` 的 `dev` 配置启动，监听 `8880`，上下文路径为 `/jeecg-boot`。
- 前端直接调用已安装的 Vite 启动器，监听 `3111`；未触发 pnpm 依赖重装，避免被未批准的依赖构建脚本阻断。
- 本机探活：`http://localhost:3111/` 与 `http://localhost:8880/jeecg-boot/sys/randomImage/abc` 均返回 HTTP 200。

下一步：在服务保持运行期间继续补齐高级 BPMN 节点运行时映射，并为新增行为增加定向后端测试。

## 2026-09-15：触发器兼容芋道已发布模型参数格式

- `WorkflowTriggerNodeDelegate` 现在同时支持当前 Jeecg 包装格式（`httpRequestSetting`/`formSettings`）和芋道已发布模型使用的直接 HTTP 配置对象、直接表单配置数组。
- HTTP 触发器的响应变量回写、表单更新、表单删除统一经过兼容解析，导入芋道简易设计器 XML 后仍可执行。
- 新增 `WorkflowTriggerNodeDelegateTest`，覆盖直接 HTTP 配置响应映射和直接表单配置数组两条运行路径。

验证说明：工作流模块定向 Maven 测试共 9 项全部通过（候选人 2、简易模型转换 5、触发器 2），未重新打包或重启服务。
下一步：继续补齐子流程多实例来源及并行/条件分支的运行时时间线验证。

## 2026-09-15：会签/或签使用稳定候选人集合变量

- 简易设计器生成的多实例审批节点现在统一使用 `${_workflowTaskAssignees_<节点ID>}` 作为 Flowable `inputDataItem`，每个实例仍通过节点级 `elementVariable` 绑定实际办理人。
- 流程启动和节点流转前已有的候选人解析会写入该集合变量，因此顺序审批、会签、或签可以共享同一份候选人快照；旧 XML 仍保留原候选人解析表达式兼容路径。
- 修正 `candidateParam` 为数组时的 BPMN 扩展序列化，避免保存成 Java 集合字符串（`[user-1, user-2]`），统一保存为逗号分隔值。

验证说明：定向 Maven 测试 `WorkflowCandidateResolverTest` 与 `WorkflowSimpleModelConverterTest` 共 7 项全部通过；仅编译/测试工作流模块，未重新打包或重启服务。

下一步：继续补充会签通过比例、或签首个完成、顺序审批及空候选人策略的运行时数据库冒烟验证，并核对 BPMN 高级节点配置闭环。

## 2026-09-15：抄送节点候选人写入闭环测试

- 新增 `WorkflowCopyNodeDelegateTest`，验证简易设计器抄送节点会将候选人解析结果、流程实例/定义、节点名称和租户上下文交给抄送服务持久化。
- 同时覆盖空候选人场景，确保抄送节点不会生成空的抄送记录；与芋道“抄送节点不产生待办”的运行语义保持一致。

验证说明：工作流模块定向 Maven 测试共 13 项全部通过（候选人 2、抄送 2、简易模型 5、任务分配 2、触发器 2），未重新打包或重启服务。

下一步：继续补充子流程多实例来源的运行时验证，并核对并行/条件分支完成后的流程时间线与任务状态。

## 2026-09-15：候选人策略基础覆盖测试补充

- 扩展候选人解析单元测试，覆盖角色（10）、部门成员（20）、岗位（22）和指定成员（30）四类常用策略，并确认禁用用户会从普通候选人集合中过滤。
- 保留连续部门负责人层级与环路防护测试，当前常用候选人策略和组织层级解析已有可重复的自动化回归入口。

验证说明：定向 Maven 测试共 5 项全部通过（候选人策略 3、抄送 2），未重新打包或重启服务。

下一步：继续为会签通过比例、顺序审批和空候选人处理增加 Flowable 引擎级运行时验证，而不是只验证 XML 转换结果。

## 2026-09-15：会签/或签/顺序审批引擎级验证

- 新增 `WorkflowMultiInstanceEngineTest`，使用 Flowable 8 内存引擎部署简易设计器实际生成的 BPMN。
- 验证或签首个完成后流程结束并取消剩余实例、会签达到 60% 后结束并取消剩余实例、顺序审批只创建下一个办理人任务。
- 工作流测试模块增加测试作用域 H2 驱动；不会进入生产运行包，也不改变现有 MySQL 配置。

验证说明：3 项 Flowable 引擎级多实例测试全部通过；测试使用独立随机内存库，未重新打包或重启开发服务。

下一步：继续验证空候选人处理、自动通过/自动拒绝与多实例组合，随后推进并行/条件分支和子流程时间线。

## 2026-09-15：回调触发增加租户隔离

- `WorkflowEngineServiceImpl.triggerCallback` 现在先按当前租户校验流程实例，再查找并触发 ReceiveTask，流程实例和任务 Key 均会去除首尾空白。
- 新增授权与越权测试，确保其他租户不能仅凭流程实例 ID 触发回调，也确保当前租户的合法回调行为不受影响。

验证说明：Flowable 多实例引擎测试 3 项、回调安全测试 2 项全部通过；未重新打包或重启服务。

下一步：补充空候选人、自动通过/自动拒绝与多实例组合的引擎级验证，并继续覆盖消息/信号边界事件的实际时间线。

## 2026-09-15：工作流目标重新盘点

- 按当前代码和测试证据重新区分已具备、部分具备和待完成项，未将“定向单元测试通过”误判为端到端完成。
- 后续工作收敛为 10 个可验收目标：多实例运行时、候选人策略、流转操作、抄送/触发器、设计器配置、表单权限、子流程与分支时间线、签名打印报表、独立启动器、菜单与端到端验收。

验证说明：当前工作流模块定向测试 13 项通过；前端 3111、后端 8880 探活正常，但尚未完成数据库级多实例冒烟和全部页面端到端验收。

下一步：优先完成多实例 Flowable 引擎级测试，再按目标清单逐项闭环并记录验收证据。

## 2026-09-15：自动审批节点运行测试

- `WorkflowTaskAssignmentEventListenerTest` 新增自动通过、自动拒绝两条节点事件路径，确认节点扩展配置会调用统一的 `completeAutomatically` 服务。
- 原有任务取消场景测试保留，自动审批测试与取消测试共 4 项全部通过；本次只做工作流模块定向测试，未重新打包或重启服务。

下一步：继续补空候选人和自动审批与多实例组合的引擎级验证，并覆盖消息/信号边界事件时间线。

## 2026-09-15：审批人为空的指定成员/流程管理员兜底

- `WorkflowCandidateResolver` 新增空审批人候选集合运行时入口，复用指定成员和流程管理员的租户感知解析逻辑。
- `WorkflowTaskAssignmentEventListener` 对原生 BPMN 中缺少候选人策略的用户任务，也会在审批人为空时写入候选用户；自动通过、自动拒绝路径保持不变。
- 新增回归测试，验证空审批人配置为指定成员时会生成全部候选用户，而不是留下无办理人的待办。

验证说明：工作流模块定向 Maven 测试 8 项全部通过（候选人策略 3、任务分配 5）；未重新打包或重启服务。

下一步：继续补充流程管理员兜底、空候选人与多实例组合的 Flowable 引擎级验证，并推进超时/边界事件真实时间线。

## 2026-09-15：超时与延迟边界事件运行路径验证

- 新增 `WorkflowTimeoutEventListenerTest`，覆盖用户任务超时自动通过、超时提醒、延迟定时器唤醒等待执行三条 Flowable 事件路径。
- 测试确认监听器会识别边界事件扩展类型，并分别调用自动审批服务、写入超时评论或触发等待 execution；未改变生产定时器配置。

验证说明：工作流模块定向 Maven 测试 3 项全部通过；未重新打包或重启服务。

下一步：继续补充超时自动拒绝、子流程超时和消息/信号边界事件的引擎级时间线，再核对退回/撤回/跳过与这些事件的组合行为。

## 2026-09-15：独立工作流启动器模块编译验证

- `jeecg-workflow-starter` 已纳入工作流聚合模块，使用独立的 `WorkflowStandaloneApplication` 和可替换身份适配器，不依赖单体 `jeecg-system-start` 启动类。
- 启动器默认端口和外部身份映射配置保持在自身模块中，单体工作流运行方式不受影响。

验证说明：执行工作流启动器及其依赖模块的 Maven `compile`，10 个反应堆模块全部成功；未打包、未重启服务。

下一步：补充启动器最小配置下的 Spring Context 启动冒烟，并继续完成子流程超时、消息/信号事件和审批操作组合验证。

## 2026-09-15：超时自动拒绝与子流程超时清理验证

- 扩展 `WorkflowTimeoutEventListenerTest`，新增用户任务超时自动拒绝和调用子流程超时清理两条路径。
- 子流程超时测试确认只删除当前调用活动下的子流程实例，并使用清理上下文避免取消事件向已删除任务写变量。

验证说明：超时/边界事件定向测试共 5 项全部通过；未重新打包或重启服务。

下一步：补充独立启动器最小配置的 Spring Context 冒烟，随后验证消息/信号事件以及退回、撤回、跳过与超时的组合时间线。

## 2026-09-15：消息/信号触发器租户隔离验证

- 新增 `WorkflowEventTriggerSecurityTest`，覆盖当前租户消息订阅携带业务变量、信号订阅调用、跨租户流程实例拒绝三条路径。
- 测试夹具改用非系统保留变量名，避免把非法变量名误判为触发器实现问题。

验证说明：工作流模块定向 Maven 测试 3 项全部通过；本轮仅编译/测试工作流模块，未重新打包或重启服务。

下一步：补充独立启动器最小配置的 Spring Context 冒烟，并验证退回、撤回、跳过与消息/信号/超时事件的组合时间线。

## 2026-09-15：可运行工作流版本 GitHub 快照

- 整理当前工作流后端、前端、独立启动器、安装 SQL、Flyway 增量脚本和运行文档，形成可追踪的 `dev` 分支快照。
- 排除 `.vite`、`outputs`、运行日志、生成配置目录和本机协作说明，避免将构建产物及本地环境内容带入仓库。
- 当前快照保留已经通过的候选人、多实例、自动审批、超时、回调及消息/信号定向测试代码，便于其他开发者复现和继续迭代。

验证说明：提交前执行暂存区差异检查和敏感信息筛查；本次只建立并推送代码快照，不重新打包或重启服务。

下一步：补充独立启动器最小配置的 Spring Context 冒烟，并验证退回、撤回、跳过与消息/信号/超时事件的组合时间线。

## 2026-09-15：信号事件支持同租户广播

- `WorkflowEventTriggerRequest` 的流程实例编号改为：消息事件必填、信号事件可选。
- `WorkflowEngineServiceImpl.triggerEvent` 在省略流程实例时使用 Flowable 租户感知的信号广播 API；指定实例时保留原有实例级校验和触发方式。
- 事件订阅返回值改为实际触发数量，并对没有可用执行订阅的异常情况给出明确错误。
- 前端事件请求类型同步允许信号广播省略 `processInstanceId`。

验证说明：工作流模块定向 Maven 测试 4 项全部通过；本轮未重新打包或重启服务。

下一步：补充独立启动器最小配置的 Spring Context 冒烟，并验证退回、撤回、跳过与消息/信号/超时事件的组合时间线。
