# Codex 工作目标

## 核心目标

在不破坏 JeecgBoot 平台能力、权限边界和部署兼容性的前提下，以最小、可验证、可回滚的改动推进本项目。Agent 的产出不仅要“能运行”，还应让下一位开发者可以从代码、测试和文档中理解为什么这样改。

## 当前事实基线

- 平台版本：JeecgBoot 3.9.5。
- 后端：Java 17、Spring Boot 4.1.0、Maven、MyBatis-Plus、Shiro/JWT。
- 前端：Vue 3.5、TypeScript、Vite 8、Ant Design Vue 4、Pinia、pnpm。
- 运行形态：单体模式是本地开发的默认路径；仓库同时提供 Nacos、Gateway 等微服务模块和 Docker Compose 编排。
- 数据设施：MySQL 与 Redis 为基础依赖；AI 场景还可能需要 pgvector、模型凭据等可选设施。
- 业务状态：仓库已接入 `jeecg-boot-module-project` 气象科研管理业务模块，覆盖指南、申报、审查、立项、执行、验收和归档主链路；经费、成果、真实 AI 与外部系统对接仍不在当前实现范围。

## Agent 的完成标准

每项任务都应满足：

1. 需求、假设和验收条件明确；存在高影响歧义时先确认。
2. 修改位于正确模块，未把业务耦合进基础核心或启动模块。
3. 权限、数据隔离、输入验证、敏感信息和迁移风险已经检查。
4. 新行为有测试，或明确说明无法自动验证的原因和人工验证步骤。
5. 只修改任务所需内容，未覆盖用户已有改动。
6. 文档、配置样例和实际命令保持一致。
7. 最终交付说明变更、验证结果、剩余风险，不声称未执行的检查已通过。

## 推荐执行循环

```text
理解目标 → 检查仓库事实 → 定义验收 → 最小实现 → 定向验证 → 审查差异 → 更新文档
```

优先选择单体模式完成普通业务开发；只有需求明确涉及独立伸缩、隔离部署、服务治理或已有微服务边界时，才扩展 Spring Cloud 模块。代码生成器适合创建 CRUD 骨架，生成结果必须经过人工审查、合并和测试，不能把生成代码视为可信成品。

## 非目标

- 不自行定义天气或其他业务域需求。
- 不为“未来可能需要”增加抽象、配置项、服务或依赖。
- 不无任务依据地升级 JeecgBoot、Spring、Vue、Vite 或数据库版本。
- 不绕过 Shiro/JWT、菜单按钮权限、数据权限或租户隔离。
- 不提交本地凭据、构建产物、日志、IDE 状态或依赖目录。
- 不自动提交、推送、发布或更改外部系统状态，除非用户明确授权。

## 权威信息顺序

发生冲突时按以下顺序判断：

1. 用户当前明确要求和验收条件；
2. 根目录 [AGENTS.md](AGENTS.md) 及更近目录中的 Agent 指令；
3. 当前仓库的构建文件、配置和可执行测试；
4. 本仓库 `docs/` 文档；
5. [JEECG 官方文档](https://help.jeecg.com/java/)；
6. 通用框架惯例。

官方文档会随上游更新；若其版本与本仓库不同，以本仓库实际版本和配置为准，并在变更说明中指出差异。
